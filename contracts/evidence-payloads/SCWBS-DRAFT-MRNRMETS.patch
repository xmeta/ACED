diff --git a/contracts/tasks/SCWBS-DRAFT-MRNRMETS.yaml b/contracts/tasks/SCWBS-DRAFT-MRNRMETS.yaml
new file mode 100644
index 0000000..c5fe42d
--- /dev/null
+++ b/contracts/tasks/SCWBS-DRAFT-MRNRMETS.yaml
@@ -0,0 +1,52 @@
+id: SCWBS-DRAFT-MRNRMETS
+type: task-contract
+wbsNodeId: node-governance-maintenance
+featureId: F-MRNRMETS
+branchName: task/SCWBS-DRAFT-MRNRMETS-issue-220-shared-lifecycle-integration-test-slee
+managedContractPaths:
+  - contracts/tasks/SCWBS-DRAFT-MRNRMETS.yaml
+  - contracts/tasks/index.yaml
+  - contracts/registry.yaml
+  - contracts/blocks/SCWBS-DRAFT-MRNRMETS.yaml
+  - contracts/approvals/SCWBS-DRAFT-MRNRMETS.yaml
+  - contracts/evidence/SCWBS-DRAFT-MRNRMETS.yaml
+  - contracts/reviews/SCWBS-DRAFT-MRNRMETS.yaml
+allowedPaths:
+  - tests/integration/finish.test.ts
+forbiddenPaths:
+  - src/**
+  - wjs/**
+  - .github/**
+  - package.json
+  - package-lock.json
+  - contracts/wbs/project.wbs.json
+  - AGENTS.md
+humanGateRequiredPaths:
+  - package.json
+  - package-lock.json
+  - tsconfig.json
+  - vitest.config.ts
+  - .github/**
+  - contracts/wbs/project.wbs.json
+  - AGENTS.md
+stopIf:
+  - package・lockfile・workflow・WBS正本・AGENTS.md・Human Gate pathの変更
+  - 仕様変更レベルの判断
+  - 固定sleep以外の共有lock仕様変更
+requiredChecks:
+  - test
+  - test:integration
+  - typecheck
+  - build
+doneCriteria:
+  - 'Complete Issue #220 shared lifecycle integration testの固定sleep raceを排除する'
+evidenceRequired:
+  - test-result
+  - typecheck-result
+  - build-result
+contractLock:
+  lockVersion: "2"
+  wbsScopeRevision: sha256:1a15147d66e7b7e66dadc0e3a3c333baf850c33d938a72bae0802619d3cb89fa
+  wbsGlobalRevision: sha256:1ec3e452163365de9e473830ef3c13ac021f4ac1df8050c9c5ccc58e78d54612
+  wbsNodeId: node-governance-maintenance
+  createdAt: "2026-07-16T17:11:07.854Z"
diff --git a/contracts/tasks/index.yaml b/contracts/tasks/index.yaml
index dbd5f0f..af6ddf7 100644
--- a/contracts/tasks/index.yaml
+++ b/contracts/tasks/index.yaml
@@ -213,3 +213,9 @@ tasks:
     wbsNodeId: node-governance-maintenance
     status: planned
     dependsOn: []
+  - id: SCWBS-DRAFT-MRNRMETS
+    path: contracts/tasks/SCWBS-DRAFT-MRNRMETS.yaml
+    branchName: task/SCWBS-DRAFT-MRNRMETS-issue-220-shared-lifecycle-integration-test-slee
+    wbsNodeId: node-governance-maintenance
+    status: planned
+    dependsOn: []
diff --git a/tests/integration/finish.test.ts b/tests/integration/finish.test.ts
index 32b1a74..6793645 100644
--- a/tests/integration/finish.test.ts
+++ b/tests/integration/finish.test.ts
@@ -30,6 +30,27 @@ function captureFinishJson(root: string, options: Parameters<typeof runFinish>[1
   }
 }
 
+function readBounded(file: string): string {
+  try {
+    return readFileSync(file, "utf8").slice(0, 2_000);
+  } catch {
+    return "<missing>";
+  }
+}
+
+async function waitForCommandStart(marker: string, lockPath: string, timeoutMs = 5_000): Promise<void> {
+  const deadline = Date.now() + timeoutMs;
+  while (Date.now() < deadline) {
+    if (readBounded(marker).split("\n").some((line) => line.startsWith("start "))) return;
+    await new Promise((resolve) => setTimeout(resolve, 10));
+  }
+  throw new Error([
+    `first wrapper did not start the fake CLI within ${timeoutMs}ms`,
+    `marker=${JSON.stringify(readBounded(marker))}`,
+    `lock=${JSON.stringify(readBounded(lockPath))}`
+  ].join("\n"));
+}
+
 function prepareFinishRepoWithHumanGate(): string {
   const root = makeTempRepo();
   writeScwbsProject(root);
@@ -225,7 +246,7 @@ describe("finish", () => {
     });
 
     const first = run();
-    await new Promise((resolve) => setTimeout(resolve, 250));
+    await waitForCommandStart(marker, path.join(root, ".git", "scwbs-command.lock"));
     const second = run();
     const results = await Promise.all([first, second]);
     expect(results.map((result) => result.code)).toEqual([0, 0]);
