diff --git a/README.md b/README.md
index 474109e..16dfab7 100644
--- a/README.md
+++ b/README.md
@@ -200,6 +200,13 @@ npm run scwbs -- finish --task <task-id>
 npm run scwbs -- registry rebuild --check
 ```
 
+`npm run typecheck` runs three checks in sequence: production TypeScript,
+test TypeScript, and the dependency-free JavaScript runners in `scripts/`.
+The production build continues to use `tsconfig.json`. Test code uses
+`tsconfig.tests.json` with no output, while scripts use `scripts/tsconfig.json`
+with `checkJs`; implicit JavaScript parameter types remain a documented
+migration boundary, but other inferred type errors are checked.
+
 For a walkthrough with decision points, use
 `docs/scwbs/getting-started.md`.
 
diff --git a/contracts/specs/SPEC-LITE-MRX7OMZL.yaml b/contracts/specs/SPEC-LITE-MRX7OMZL.yaml
new file mode 100644
index 0000000..9c617a8
--- /dev/null
+++ b/contracts/specs/SPEC-LITE-MRX7OMZL.yaml
@@ -0,0 +1,17 @@
+id: SPEC-LITE-MRX7OMZL
+type: spec-contract
+featureId: F-MRX7OMZL
+title: "Issue #247: canonical typecheckでtestsとscriptsの型安全性を検証する"
+status: approved
+version: 0.1.0
+summary: "production buildを変えず、テストTypeScriptと補助scriptを専用設定で型検査する"
+approvedBy: user-requested-baseline-continuation
+approvedAt: "2026-07-23T00:00:00+09:00"
+acceptanceCriteria:
+  - npm run typecheckはproduction sourceの検査後にtest用設定の検査を直列実行する
+  - tsconfig.tests.jsonはtests TypeScriptをincludeし、noEmitかつrepository root基準で解決する
+  - JavaScript補助scriptはcheckJsを有効化して可能な限り同じ検査へ含める
+  - 型検査対象外が残る場合は対象と理由を文書化する
+  - production tsconfig、build出力、runtime挙動は変更しない
+  - 既存のtestとintegration testを維持する
+  - package.json変更はTaskのHuman Gateとして承認要求し、人間の承認まで完了扱いにしない
diff --git a/contracts/tasks/SCWBS-DRAFT-MRX7OMZL.yaml b/contracts/tasks/SCWBS-DRAFT-MRX7OMZL.yaml
new file mode 100644
index 0000000..7fe0b66
--- /dev/null
+++ b/contracts/tasks/SCWBS-DRAFT-MRX7OMZL.yaml
@@ -0,0 +1,64 @@
+id: SCWBS-DRAFT-MRX7OMZL
+type: task-contract
+wbsNodeId: node-governance-maintenance
+featureId: F-MRX7OMZL
+branchName: codex/SCWBS-DRAFT-MRX7OMZL-issue-247-test-typecheck
+allowedPaths:
+  - package.json
+  - tsconfig.tests.json
+  - scripts/tsconfig.json
+  - tests/integration/approval-delegation.test.ts
+  - tests/integration/tasks.test.ts
+  - tests/unit/integration-test-output.test.ts
+  - tests/unit/integration-test-run.test.ts
+  - tests/unit/typecheck-config.test.ts
+  - README.md
+forbiddenPaths:
+  - wjs/**
+  - package-lock.json
+  - tsconfig.json
+  - vitest.config.ts
+  - .github/**
+  - contracts/wbs/**
+  - contracts/changesets/**
+humanGateRequiredPaths:
+  - package.json
+  - package-lock.json
+  - tsconfig.json
+  - vitest.config.ts
+  - .github/**
+requiredChecks:
+  - test
+  - test:integration
+  - typecheck
+  - build
+doneCriteria:
+  - npm run typecheckがsrcとtestsのTypeScriptを両方検査する
+  - scriptsのJavaScriptも型検査可能な範囲を明示し、未検査領域を隠さない
+  - production buildのrootDirと出力内容を変更しない
+  - test用tsconfigは既存設定を継承しnoEmitで動作する
+  - 意図的なテストfixture以外の型エラーを修正し、型抑制は理由を限定する
+  - canonical test、integration、typecheck、buildが成功する
+  - package.json変更をHuman Gate対象としてrequestし、AIは承認しない
+evidenceRequired:
+  - test-result
+  - integration-test-result
+  - typecheck-result
+  - build-result
+managedContractPaths:
+  - contracts/tasks/SCWBS-DRAFT-MRX7OMZL.yaml
+  - contracts/specs/SPEC-LITE-MRX7OMZL.yaml
+  - contracts/tasks/index.yaml
+  - contracts/registry.yaml
+  - contracts/blocks/SCWBS-DRAFT-MRX7OMZL.yaml
+  - contracts/evidence/SCWBS-DRAFT-MRX7OMZL.yaml
+  - contracts/approvals/SCWBS-DRAFT-MRX7OMZL.yaml
+  - contracts/reviews/SCWBS-DRAFT-MRX7OMZL.yaml
+contractLock:
+  lockVersion: "2"
+  wbsScopeRevision: sha256:1a15147d66e7b7e66dadc0e3a3c333baf850c33d938a72bae0802619d3cb89fa
+  wbsGlobalRevision: sha256:1ec3e452163365de9e473830ef3c13ac021f4ac1df8050c9c5ccc58e78d54612
+  wbsNodeId: node-governance-maintenance
+  specVersion: 0.1.0
+  specRevision: sha256:397ac7f10c75f93605efa6b619adb6ca5b78975efd02611bb212f70b66c050ec
+  createdAt: "2026-07-23T07:51:50.481Z"
diff --git a/contracts/tasks/index.yaml b/contracts/tasks/index.yaml
index 8953e4b..1bd06fe 100644
--- a/contracts/tasks/index.yaml
+++ b/contracts/tasks/index.yaml
@@ -1238,3 +1238,9 @@ tasks:
     status: archived
     dependsOn: []
     archivedAt: "2026-07-23T07:31:01.293Z"
+  - id: SCWBS-DRAFT-MRX7OMZL
+    path: contracts/tasks/SCWBS-DRAFT-MRX7OMZL.yaml
+    branchName: codex/SCWBS-DRAFT-MRX7OMZL-issue-247-test-typecheck
+    wbsNodeId: node-governance-maintenance
+    status: planned
+    dependsOn: []
diff --git a/package.json b/package.json
index 8cd7448..ff7d9c4 100644
--- a/package.json
+++ b/package.json
@@ -19,7 +19,7 @@
   "scripts": {
     "build": "tsc -p tsconfig.json",
     "prepack": "npm run build",
-    "typecheck": "tsc -p tsconfig.json --noEmit",
+    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json && tsc -p scripts/tsconfig.json",
     "test": "vitest run tests/unit",
     "test:integration": "node scripts/integration-test-output.mjs",
     "test:integration:verbose": "node scripts/integration-test-output.mjs --verbose",
diff --git a/scripts/tsconfig.json b/scripts/tsconfig.json
new file mode 100644
index 0000000..887816a
--- /dev/null
+++ b/scripts/tsconfig.json
@@ -0,0 +1,14 @@
+{
+  "extends": "../tsconfig.json",
+  "compilerOptions": {
+    "allowJs": true,
+    "checkJs": true,
+    "noEmit": true,
+    "noImplicitAny": false,
+    "rootDir": "..",
+    "useUnknownInCatchVariables": false
+  },
+  "include": [
+    "*.mjs"
+  ]
+}
diff --git a/tests/integration/approval-delegation.test.ts b/tests/integration/approval-delegation.test.ts
index ead3120..d86a2df 100644
--- a/tests/integration/approval-delegation.test.ts
+++ b/tests/integration/approval-delegation.test.ts
@@ -14,8 +14,11 @@ describe("approval delegation prepare", () => {
     const result = buildApprovalDelegationPrepare(root, "WBS-001-004", options, TOKEN);
     expect(result.error).toBeUndefined();
     const rendered = JSON.stringify(result.output);
-    expect(result.output?.policyPatch.approvalPolicy.tokenSha256).toMatch(/^sha256:[a-f0-9]{64}$/);
-    expect(result.output?.policyPatch.approvalPolicy.scopes).toEqual(["human-gate", "post-finish"]);
+    const approvalPolicy = result.output?.policyPatch.approvalPolicy;
+    expect(approvalPolicy?.mode).toBe("delegated");
+    if (approvalPolicy?.mode !== "delegated") throw new Error("expected delegated approval policy");
+    expect(approvalPolicy.tokenSha256).toMatch(/^sha256:[a-f0-9]{64}$/);
+    expect(approvalPolicy.scopes).toEqual(["human-gate", "post-finish"]);
     expect(result.output?.handoff.join(" ")).toContain("never auto-loads .env");
     expect(rendered).not.toContain(TOKEN);
     expect(result.output?.governanceCostProxy).toEqual({ manualInputsRequired: 5, generatedFields: 3, requiredContractOnlyCommits: 1, finishRetriesAdded: 0 });
diff --git a/tests/integration/tasks.test.ts b/tests/integration/tasks.test.ts
index 0a9f2b4..7281aed 100644
--- a/tests/integration/tasks.test.ts
+++ b/tests/integration/tasks.test.ts
@@ -11,10 +11,14 @@ import { buildLockedTask, runTaskLock } from "../../src/commands/task-lock.js";
 import { buildAffectedTaskRefreshReport, buildTaskRefreshPreview, runTaskRefresh, taskRefreshReasons } from "../../src/commands/task-refresh.js";
 import { applyWbsChangesets, buildWbsCandidatesFromTaskIndex } from "../../src/commands/wbs.js";
 import { buildNextTask } from "../../src/commands/ai-queue.js";
-import { validateJsonWithSchema } from "../../wjs/tools/validate.js";
 import { main } from "../../src/cli.js";
 import { makeTempRepo, sampleTask, sampleWbs, sampleSpec, sampleSpecChange, writeScwbsProject, writeJson, writeText, writeYaml } from "../helpers.js";
 
+const wjsValidatorModule = "../../wjs/tools/validate.js";
+const { validateJsonWithSchema } = await import(wjsValidatorModule) as {
+  validateJsonWithSchema(document: unknown, kind: "wbs" | "operations"): string[];
+};
+
 describe("task management", () => {
   test("check errors when contract lock wbs node id is stale", () => {
     const root = makeTempRepo();
diff --git a/tests/unit/integration-test-output.test.ts b/tests/unit/integration-test-output.test.ts
index 92ee48d..cf1f116 100644
--- a/tests/unit/integration-test-output.test.ts
+++ b/tests/unit/integration-test-output.test.ts
@@ -3,11 +3,8 @@ import { existsSync, mkdtempSync, rmSync, writeFileSync } from "node:fs";
 import { tmpdir } from "node:os";
 import path from "node:path";
 import { describe, expect, it } from "vitest";
-// @ts-expect-error The production output controller is intentionally dependency-free ESM.
 import { FAILURE_LIMIT, formatFailureDiagnostics, parseArgs } from "../../scripts/integration-test-output.mjs";
-// @ts-expect-error The production runner is intentionally dependency-free ESM.
 import { formatSummary, formatTempDiagnostic, normalizeReport } from "../../scripts/integration-test-run.mjs";
-// @ts-expect-error The production lock helper is intentionally dependency-free ESM.
 import {
   acquireIntegrationRun,
   integrationLockPath,
diff --git a/tests/unit/integration-test-run.test.ts b/tests/unit/integration-test-run.test.ts
index c770e72..5e28141 100644
--- a/tests/unit/integration-test-run.test.ts
+++ b/tests/unit/integration-test-run.test.ts
@@ -1,5 +1,4 @@
 import { describe, expect, it } from "vitest";
-// @ts-expect-error The production runner is intentionally a dependency-free ESM script.
 import {
   SERIAL_WITHIN_FILE,
   buildVitestArgs,
@@ -94,7 +93,7 @@ describe("integration test runner", () => {
     };
     const report = normalizeReport(raw, 2500, 4);
     const summary = formatSummary(report, 1);
-    expect(report.files.map((entry) => entry.file)).toEqual(["tests/integration/slow.test.ts", "tests/integration/fast.test.ts"]);
+    expect(report.files.map((entry: { file: string }) => entry.file)).toEqual(["tests/integration/slow.test.ts", "tests/integration/fast.test.ts"]);
     expect(report.tests[0]).toMatchObject({ name: "slow test", durationMs: 1900 });
     expect(summary.split("\n")).toHaveLength(3);
     expect(summary).toContain("duration=2.50s");
diff --git a/tests/unit/typecheck-config.test.ts b/tests/unit/typecheck-config.test.ts
new file mode 100644
index 0000000..41d3f94
--- /dev/null
+++ b/tests/unit/typecheck-config.test.ts
@@ -0,0 +1,39 @@
+import { readFileSync } from "node:fs";
+import { describe, expect, test } from "vitest";
+
+function readJson(path: string): Record<string, unknown> {
+  return JSON.parse(readFileSync(path, "utf8")) as Record<string, unknown>;
+}
+
+describe("canonical typecheck configuration", () => {
+  test("checks production, tests, and JavaScript runners without changing the build", () => {
+    const packageJson = readJson("package.json");
+    const scripts = packageJson.scripts as Record<string, string>;
+
+    expect(scripts.build).toBe("tsc -p tsconfig.json");
+    expect(scripts.typecheck).toBe(
+      "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json && tsc -p scripts/tsconfig.json"
+    );
+  });
+
+  test("includes all test TypeScript with no output", () => {
+    const config = readJson("tsconfig.tests.json");
+    const compilerOptions = config.compilerOptions as Record<string, unknown>;
+
+    expect(config.include).toEqual(["tests/**/*.ts"]);
+    expect(config.exclude).toBeUndefined();
+    expect(compilerOptions.noEmit).toBe(true);
+    expect(compilerOptions.rootDir).toBe(".");
+  });
+
+  test("checks JavaScript runners while recording the implicit-any migration boundary", () => {
+    const config = readJson("scripts/tsconfig.json");
+    const compilerOptions = config.compilerOptions as Record<string, unknown>;
+
+    expect(config.include).toEqual(["*.mjs"]);
+    expect(compilerOptions.allowJs).toBe(true);
+    expect(compilerOptions.checkJs).toBe(true);
+    expect(compilerOptions.noEmit).toBe(true);
+    expect(compilerOptions.noImplicitAny).toBe(false);
+  });
+});
diff --git a/tsconfig.tests.json b/tsconfig.tests.json
new file mode 100644
index 0000000..c7dfbe1
--- /dev/null
+++ b/tsconfig.tests.json
@@ -0,0 +1,15 @@
+{
+  "extends": "./tsconfig.json",
+  "compilerOptions": {
+    "allowJs": true,
+    "checkJs": false,
+    "allowImportingTsExtensions": true,
+    "module": "ESNext",
+    "moduleResolution": "Bundler",
+    "noEmit": true,
+    "rootDir": "."
+  },
+  "include": [
+    "tests/**/*.ts"
+  ]
+}
