diff --git a/.github/workflows/scwbs.yml b/.github/workflows/scwbs.yml
index 2151a2a..4c40b41 100644
--- a/.github/workflows/scwbs.yml
+++ b/.github/workflows/scwbs.yml
@@ -307,6 +307,15 @@ jobs:
         run: npm run lint
       - name: Unit tests
         run: npm test
+      - name: Combined unit and integration coverage
+        run: npm run test:coverage:all
+      - name: Upload combined coverage report
+        if: always()
+        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
+        with:
+          name: coverage-${{ github.sha }}
+          path: coverage
+          if-no-files-found: warn
       - name: Typecheck
         run: npm run typecheck
       - name: Build
diff --git a/README.md b/README.md
index 67f9ce4..1153345 100644
--- a/README.md
+++ b/README.md
@@ -234,12 +234,19 @@ Tests are split into two groups:
 - `tests/unit/` – fast, lightweight tests used by the default `npm test` command.
 - `tests/integration/` – heavier tests that create temporary Git repositories.
 
+Coverage commands use the same two scopes:
+
+- `npm run test:coverage` – unit-only coverage for fast local feedback.
+- `npm run test:coverage:all` – combined unit and integration coverage using the V8 provider; the report is written to `coverage/`. The deliberately large receipt-bound stress case is excluded from this instrumented run because its per-test timeout is lower than its synchronous filesystem workload; the complete integration suite remains covered by `npm run test:integration`.
+
 Run the full local verification set:
 
 ```bash
 npm test                    # unit tests only (fast)
 npm run test:integration    # integration tests (heavier)
 npm run test:all            # all tests
+npm run test:coverage       # unit-only coverage (fast)
+npm run test:coverage:all   # unit and integration coverage
 npm run lint                # ESLint baseline gate (currently up to 37 warnings)
 npm run format              # format source, tests, scripts, docs, and root config files
 npm run typecheck
diff --git a/contracts/blocks/SCWBS-DRAFT-MSDEL9HS.yaml b/contracts/blocks/SCWBS-DRAFT-MSDEL9HS.yaml
new file mode 100644
index 0000000..0637f74
--- /dev/null
+++ b/contracts/blocks/SCWBS-DRAFT-MSDEL9HS.yaml
@@ -0,0 +1,21 @@
+id: BLK-SCWBS-DRAFT-MSDEL9HS
+type: block
+taskId: SCWBS-DRAFT-MSDEL9HS
+status: resolved
+category: human-gate
+level: 1
+requiredHumanDecision: Complete the required human gate decision before implementation continues.
+reason: "Human Gate required: Issue #375 requires package.json and .github/workflows/scwbs.yml changes for the combined coverage command and CI artifact policy; AI must stop before implementation"
+createdAt: "2026-08-03T15:50:17.277Z"
+history:
+  - status: blocked
+    at: "2026-08-03T15:50:17.277Z"
+    reason: "Human Gate required: Issue #375 requires package.json and .github/workflows/scwbs.yml changes for the combined coverage command and CI artifact policy; AI must stop before implementation"
+    by: ai-agent
+  - status: resolved
+    at: "2026-08-03T16:31:25.500Z"
+    reason: カバレッジscriptとCI artifact方針を承認
+    by: human
+resolvedAt: "2026-08-03T16:31:25.500Z"
+resolvedBy: human
+resolution: カバレッジscriptとCI artifact方針を承認
diff --git a/contracts/specs/SPEC-LITE-MSDEL9HS.yaml b/contracts/specs/SPEC-LITE-MSDEL9HS.yaml
new file mode 100644
index 0000000..2e9a3ed
--- /dev/null
+++ b/contracts/specs/SPEC-LITE-MSDEL9HS.yaml
@@ -0,0 +1,14 @@
+id: SPEC-LITE-MSDEL9HS
+type: spec-contract
+featureId: F-MSDEL9HS
+title: "GitHub Issue #375: measure effective unit and integration test coverage in CI"
+status: approved
+version: 0.1.0
+approvedBy: user-requested-github-issue-375
+approvedAt: "2026-08-04T00:00:00Z"
+summary: "GitHub Issue #375: measure effective unit and integration test coverage in CI"
+acceptanceCriteria:
+  - 'Add a combined coverage command that includes both tests/unit and tests/integration while preserving the existing unit-only command.'
+  - 'Run combined coverage in CI and upload the generated coverage report as an artifact without turning coverage into a blocking threshold gate.'
+  - 'Document the two coverage scopes and update the implementation-gap statement so coverage is described as continuously measured, not threshold-enforced.'
+  - 'Keep the existing integration runner, test semantics, and required CI check names unchanged.'
diff --git a/contracts/tasks/SCWBS-DRAFT-MSDEL9HS.yaml b/contracts/tasks/SCWBS-DRAFT-MSDEL9HS.yaml
new file mode 100644
index 0000000..c125d71
--- /dev/null
+++ b/contracts/tasks/SCWBS-DRAFT-MSDEL9HS.yaml
@@ -0,0 +1,60 @@
+id: SCWBS-DRAFT-MSDEL9HS
+type: task-contract
+wbsNodeId: node-governance-maintenance
+featureId: F-MSDEL9HS
+branchName: task/SCWBS-DRAFT-MSDEL9HS-issue-375-effective-coverage-ci
+allowedPaths:
+  - package.json
+  - .github/workflows/scwbs.yml
+  - README.md
+  - docs/implementation-gaps.md
+  - contracts/tasks/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/specs/SPEC-LITE-MSDEL9HS.yaml
+  - contracts/tasks/index.yaml
+  - contracts/blocks/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/evidence/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/evidence-payloads/SCWBS-DRAFT-MSDEL9HS.patch
+  - contracts/approvals/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/reviews/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/registry.yaml
+forbiddenPaths:
+  - wjs/**
+humanGateRequiredPaths:
+  - package.json
+  - package-lock.json
+  - tsconfig.json
+  - vitest.config.ts
+  - .github/**
+requiredChecks:
+  - test
+  - test:integration
+  - typecheck
+  - build
+doneCriteria:
+  - '`test:coverage` remains the documented unit-only fast path.'
+  - '`test:coverage:all` measures both unit and integration tests with the existing V8 coverage provider and produces a reviewable report.'
+  - CI runs the combined coverage command and retains its report as an artifact without adding a coverage threshold gate.
+  - README and docs/implementation-gaps.md distinguish unit-only and combined coverage, and record the CI measurement boundary.
+  - No source behavior, integration runner semantics, or coverage threshold policy changes are introduced.
+evidenceRequired:
+  - test-result
+  - typecheck-result
+  - build-result
+managedContractPaths:
+  - contracts/tasks/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/specs/SPEC-LITE-MSDEL9HS.yaml
+  - contracts/tasks/index.yaml
+  - contracts/blocks/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/evidence/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/evidence-payloads/SCWBS-DRAFT-MSDEL9HS.patch
+  - contracts/approvals/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/reviews/SCWBS-DRAFT-MSDEL9HS.yaml
+  - contracts/registry.yaml
+contractLock:
+  lockVersion: "2"
+  wbsScopeRevision: sha256:1a15147d66e7b7e66dadc0e3a3c333baf850c33d938a72bae0802619d3cb89fa
+  wbsGlobalRevision: sha256:1ec3e452163365de9e473830ef3c13ac021f4ac1df8050c9c5ccc58e78d54612
+  wbsNodeId: node-governance-maintenance
+  specVersion: 0.1.0
+  specRevision: sha256:f92275dda5930a84e40f569cd9fe8e8add7034296a422a5ce011764070d78dd9
+  createdAt: "2026-08-03T15:49:18.931Z"
diff --git a/docs/implementation-gaps.md b/docs/implementation-gaps.md
index 3b8af5a..2573f4e 100644
--- a/docs/implementation-gaps.md
+++ b/docs/implementation-gaps.md
@@ -29,6 +29,7 @@ This document tracks the pieces that are intentionally still missing from the cu
 - Repository dogfooding with `contracts/wbs/project.wbs.json` and active Task Contracts
 - WBS status summary
 - WJS semantic apply wrapper
+- Unit and integration test coverage measurement, including a CI-retained report
 
 ## Still Missing
 
@@ -40,7 +41,7 @@ This document tracks the pieces that are intentionally still missing from the cu
 | Risk management | Risk Register format and command | Strict workflows still need a formal risk log |
 | Evidence trust | External artifact signatures and independent CI attestation | Tracked patch retention now reconstructs subject trees locally, but artifact signing remains out of scope |
 | Evidence diff basis | CI correlation and publish-time PR metadata gating | `evidence collect` records branch-diff provenance and can capture PR numbers, but CI run correlation and a dedicated publish gate are not wired into the workflow yet |
-| Test quality | Diff-aware assertion and coverage inspection | `testQuality` can be recorded as Evidence metadata, but source diffs and coverage reports are not parsed yet |
+| Test quality | Diff-aware assertion and coverage inspection | Unit-only and combined unit/integration coverage reports are measured locally and in CI, but source diffs and coverage reports are not parsed and no threshold gate is enforced |
 | Health checks | Timestamp-based drift detection for code vs contracts | `scwbs health` does not yet prove freshness from history |
 | Review independence | Separate independent review mode | Single-session review remains policy-driven; `scwbs check` does not enforce independent review validation |
 | CI integration | PR feedback and CI evidence correlation | GitHub Actions run the local checks, but PR feedback and Evidence links to CI runs are not wired into the workflow yet |
diff --git a/package.json b/package.json
index 5b15314..58fca7b 100644
--- a/package.json
+++ b/package.json
@@ -27,6 +27,7 @@
     "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json && tsc -p scripts/tsconfig.json",
     "test": "vitest run tests/unit",
     "test:coverage": "vitest run tests/unit --coverage",
+    "test:coverage:all": "vitest run tests/unit tests/integration --coverage --pool=forks --maxWorkers=4 --no-file-parallelism --testTimeout=60000 -t='^(?!.*bounds events per Task and receipts per repository)'",
     "test:integration": "node scripts/integration-test-output.mjs",
     "test:integration:verbose": "node scripts/integration-test-output.mjs --verbose",
     "test:wjs": "npm test -w wjs",
