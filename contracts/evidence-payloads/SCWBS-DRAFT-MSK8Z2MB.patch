diff --git a/.github/actions/setup-toolchain/action.yml b/.github/actions/setup-toolchain/action.yml
new file mode 100644
index 0000000..b52d6fd
--- /dev/null
+++ b/.github/actions/setup-toolchain/action.yml
@@ -0,0 +1,38 @@
+name: Set up Node.js toolchain
+description: Check out the repository, configure Node.js and Corepack, and install dependencies
+inputs:
+  node-version:
+    description: Node.js version to install
+    required: true
+  checkout-ref:
+    description: Optional ref to check out
+    required: false
+    default: ""
+  fetch-depth:
+    description: Git fetch depth
+    required: false
+    default: "1"
+runs:
+  using: composite
+  steps:
+    - name: Checkout repository
+      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
+      with:
+        ref: ${{ inputs.checkout-ref }}
+        fetch-depth: ${{ inputs.fetch-depth }}
+        submodules: recursive
+    - name: Set up Node.js
+      uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
+      with:
+        node-version: ${{ inputs.node-version }}
+        cache: npm
+    - name: Enable Corepack and report toolchain
+      shell: bash
+      run: |
+        npm install --global corepack@0.35.0
+        corepack enable
+        node --version
+        corepack npm --version
+    - name: Install dependencies
+      shell: bash
+      run: corepack npm ci --no-audit --no-fund
diff --git a/.github/workflows/scwbs.yml b/.github/workflows/scwbs.yml
index f0760e4..b0e9a9c 100644
--- a/.github/workflows/scwbs.yml
+++ b/.github/workflows/scwbs.yml
@@ -17,36 +17,27 @@ on:
 jobs:
   plan:
     runs-on: ubuntu-latest
+    env:
+      SCWBS_APPROVAL_DELEGATION_TOKEN: ${{ secrets.SCWBS_APPROVAL_DELEGATION_TOKEN }}
     outputs:
       mode: ${{ steps.final.outputs.mode }}
       task_id: ${{ steps.final.outputs.task_id }}
       subject: ${{ steps.final.outputs.subject }}
     steps:
-      - name: Checkout exact head with full history
+      - name: Bootstrap local action checkout
         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
+      - name: Set up repository and Node.js toolchain
+        uses: ./.github/actions/setup-toolchain
         with:
-          ref: ${{ github.event.pull_request.head.sha || github.sha }}
-          fetch-depth: 0
-          submodules: recursive
+          node-version: ${{ env.NODE_VERSION }}
+          checkout-ref: ${{ github.event.pull_request.head.sha || github.sha }}
+          fetch-depth: "0"
       - name: Restore branch identity and base reference
         env:
           CI_BRANCH: ${{ github.head_ref || github.ref_name }}
         run: |
           git switch -C "$CI_BRANCH" HEAD
           git fetch origin main:refs/remotes/origin/main
-      - name: Set up Node.js
-        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
-        with:
-          node-version: ${{ env.NODE_VERSION }}
-          cache: npm
-      - name: Enable Corepack and report toolchain
-        run: |
-          npm install --global corepack@0.35.0
-          corepack enable
-          node --version
-          corepack npm --version
-      - name: Install dependencies
-        run: corepack npm ci --no-audit --no-fund
       - name: Build structural CI plan
         id: candidate
         env:
@@ -256,24 +247,14 @@ jobs:
       matrix:
         node-version: ["22.12.0"]
     steps:
-      - name: Checkout repository
+      - name: Bootstrap local action checkout
         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
-        with:
-          submodules: recursive
-      - name: Set up minimum supported Node.js
-        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
+      - name: Set up repository and Node.js toolchain
+        uses: ./.github/actions/setup-toolchain
         with:
           node-version: ${{ matrix.node-version }}
-          cache: npm
-      - name: Enable Corepack and report toolchain
-        run: |
-          npm install --global corepack@0.35.0
-          corepack enable
-          node --version
-          corepack npm --version
-      - name: Install dependencies and run core tests
+      - name: Run core tests
         run: |
-          corepack npm ci --no-audit --no-fund
           npm run lint
           npm test
           npm run typecheck
@@ -284,23 +265,12 @@ jobs:
     if: always() && needs.plan.outputs.mode == 'full'
     runs-on: ubuntu-latest
     steps:
-      - name: Checkout repository
+      - name: Bootstrap local action checkout
         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
-        with:
-          submodules: recursive
-      - name: Set up Node.js
-        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
+      - name: Set up repository and Node.js toolchain
+        uses: ./.github/actions/setup-toolchain
         with:
           node-version: ${{ env.NODE_VERSION }}
-          cache: npm
-      - name: Enable Corepack and report toolchain
-        run: |
-          npm install --global corepack@0.35.0
-          corepack enable
-          node --version
-          corepack npm --version
-      - name: Install dependencies
-        run: corepack npm ci --no-audit --no-fund
       - name: Verify npm workspaces
         run: npm ls --workspaces --depth=0
       - name: Lint
@@ -360,23 +330,12 @@ jobs:
     if: always() && needs.plan.outputs.mode == 'full'
     runs-on: ubuntu-latest
     steps:
-      - name: Checkout repository
+      - name: Bootstrap local action checkout
         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
-        with:
-          submodules: recursive
-      - name: Set up Node.js
-        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
+      - name: Set up repository and Node.js toolchain
+        uses: ./.github/actions/setup-toolchain
         with:
           node-version: ${{ env.NODE_VERSION }}
-          cache: npm
-      - name: Enable Corepack and report toolchain
-        run: |
-          npm install --global corepack@0.35.0
-          corepack enable
-          node --version
-          corepack npm --version
-      - name: Install dependencies
-        run: corepack npm ci --no-audit --no-fund
       - name: Integration tests
         run: npm run test:integration
 
@@ -385,23 +344,12 @@ jobs:
     if: always() && needs.plan.outputs.mode == 'full'
     runs-on: ubuntu-latest
     steps:
-      - name: Checkout repository
+      - name: Bootstrap local action checkout
         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
-        with:
-          submodules: recursive
-      - name: Set up Node.js
-        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
+      - name: Set up repository and Node.js toolchain
+        uses: ./.github/actions/setup-toolchain
         with:
           node-version: ${{ env.NODE_VERSION }}
-          cache: npm
-      - name: Enable Corepack and report toolchain
-        run: |
-          npm install --global corepack@0.35.0
-          corepack enable
-          node --version
-          corepack npm --version
-      - name: Install dependencies
-        run: corepack npm ci --no-audit --no-fund
       - name: WJS tests
         run: npm run test:wjs
 
diff --git a/contracts/tasks/SCWBS-DRAFT-MSK8Z2MB.yaml b/contracts/tasks/SCWBS-DRAFT-MSK8Z2MB.yaml
new file mode 100644
index 0000000..ac73829
--- /dev/null
+++ b/contracts/tasks/SCWBS-DRAFT-MSK8Z2MB.yaml
@@ -0,0 +1,63 @@
+id: SCWBS-DRAFT-MSK8Z2MB
+type: task-contract
+wbsNodeId: node-governance-maintenance
+featureId: F-MSK8Z2MB
+branchName: task/SCWBS-DRAFT-MSK8Z2MB-issue-446-deduplicate-ci-toolchain-setup
+allowedPaths:
+  - .github/workflows/scwbs.yml
+  - .github/actions/setup-toolchain/action.yml
+forbiddenPaths:
+  - src/auth/**
+  - src/database/**
+  - contracts/wbs/project.wbs.json
+  - wjs/**
+humanGateRequiredPaths:
+  - package.json
+  - package-lock.json
+  - tsconfig.json
+  - vitest.config.ts
+  - .github/**
+stopIf:
+  - schema change required
+  - dependency change required
+  - authentication or permission change required
+  - breaking API change required
+  - external service or release decision required
+requiredChecks:
+  - test
+  - typecheck
+  - build
+  - lint
+  - test:integration
+doneCriteria:
+  - 'Complete Issue #446: deduplicate CI toolchain setup'
+evidenceRequired:
+  - test-result
+  - typecheck-result
+  - build-result
+approvalPolicy:
+  mode: delegated
+  delegatedBy: xmeta
+  delegatedTo: ai-agent
+  scopes:
+    - human-gate
+    - post-finish
+  source: https://github.com/xmeta/ACED/issues/446
+  reason: "xmeta delegates bounded human-gate and post-finish approval for Issue #446"
+  expiresAt: "2026-12-31T00:00:00.000Z"
+  tokenSha256: sha256:e3d7c44606a58c4624550061137669f4dc7d1f00e941f50b5f09e0459275f9c5
+managedContractPaths:
+  - contracts/tasks/SCWBS-DRAFT-MSK8Z2MB.yaml
+  - contracts/tasks/index.yaml
+  - contracts/blocks/SCWBS-DRAFT-MSK8Z2MB.yaml
+  - contracts/evidence/SCWBS-DRAFT-MSK8Z2MB.yaml
+  - contracts/evidence-payloads/SCWBS-DRAFT-MSK8Z2MB.patch
+  - contracts/approvals/SCWBS-DRAFT-MSK8Z2MB.yaml
+  - contracts/reviews/SCWBS-DRAFT-MSK8Z2MB.yaml
+  - contracts/registry.yaml
+contractLock:
+  lockVersion: "2"
+  wbsScopeRevision: sha256:1a15147d66e7b7e66dadc0e3a3c333baf850c33d938a72bae0802619d3cb89fa
+  wbsGlobalRevision: sha256:1ec3e452163365de9e473830ef3c13ac021f4ac1df8050c9c5ccc58e78d54612
+  wbsNodeId: node-governance-maintenance
+  createdAt: "2026-08-08T10:45:36.249Z"
diff --git a/contracts/tasks/index.yaml b/contracts/tasks/index.yaml
index 71139d9..79d2293 100644
--- a/contracts/tasks/index.yaml
+++ b/contracts/tasks/index.yaml
@@ -1686,3 +1686,9 @@ tasks:
     status: archived
     dependsOn: []
     archivedAt: "2026-08-08T10:43:05.216Z"
+  - id: SCWBS-DRAFT-MSK8Z2MB
+    path: contracts/tasks/SCWBS-DRAFT-MSK8Z2MB.yaml
+    branchName: task/SCWBS-DRAFT-MSK8Z2MB-issue-446-deduplicate-ci-toolchain-setup
+    wbsNodeId: node-governance-maintenance
+    status: planned
+    dependsOn: []
