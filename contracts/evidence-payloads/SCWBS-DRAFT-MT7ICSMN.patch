diff --git a/contracts/approvals/SCWBS-041.yaml b/contracts/approvals/SCWBS-041.yaml
index a2cf5c9..69864a1 100644
--- a/contracts/approvals/SCWBS-041.yaml
+++ b/contracts/approvals/SCWBS-041.yaml
@@ -1,8 +1,33 @@
 id: APR-SCWBS-041
 type: approval
 taskId: SCWBS-041
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:19:18.853Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:45:03.496Z"
+    approvalMode: human
+    headCommit: 573796f072b40fce28017ac75d972b912c04941b
+    diffHash: sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
+    pullRequest: "#43"
+    reason: CONFIRM TTY APPROVAL SCWBS-041 post-finish 573796f072b40fce28017ac75d972b912c04941b sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:45:03.495Z"
+    verificationLevel: lean
 status: approved
-approvedBy: human
-approvedAt: "2026-07-02T18:40:00.171Z"
+requestedAt: "2026-08-24T19:19:18.853Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:45:03.496Z"
+approvalMode: human
+headCommit: 573796f072b40fce28017ac75d972b912c04941b
+diffHash: sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
 pullRequest: "#43"
-reason: User delegated human completion review and accepted evidence
+reason: CONFIRM TTY APPROVAL SCWBS-041 post-finish 573796f072b40fce28017ac75d972b912c04941b sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:45:03.495Z"
+verificationLevel: lean
diff --git a/contracts/approvals/SCWBS-042.yaml b/contracts/approvals/SCWBS-042.yaml
index 06b9c6a..b4902d8 100644
--- a/contracts/approvals/SCWBS-042.yaml
+++ b/contracts/approvals/SCWBS-042.yaml
@@ -1,8 +1,33 @@
 id: APR-SCWBS-042
 type: approval
 taskId: SCWBS-042
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:19:40.814Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:45:20.933Z"
+    approvalMode: human
+    headCommit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+    diffHash: sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
+    pullRequest: "#44"
+    reason: CONFIRM TTY APPROVAL SCWBS-042 post-finish b832efb9d0c29bb3db38729d472a296c3b8bb528 sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:45:20.932Z"
+    verificationLevel: lean
 status: approved
-approvedBy: human
-approvedAt: "2026-07-02T18:40:00.871Z"
+requestedAt: "2026-08-24T19:19:40.814Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:45:20.933Z"
+approvalMode: human
+headCommit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+diffHash: sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
 pullRequest: "#44"
-reason: User delegated human completion review and accepted evidence
+reason: CONFIRM TTY APPROVAL SCWBS-042 post-finish b832efb9d0c29bb3db38729d472a296c3b8bb528 sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:45:20.932Z"
+verificationLevel: lean
diff --git a/contracts/approvals/SCWBS-043.yaml b/contracts/approvals/SCWBS-043.yaml
index 1e9dba3..757e391 100644
--- a/contracts/approvals/SCWBS-043.yaml
+++ b/contracts/approvals/SCWBS-043.yaml
@@ -1,8 +1,33 @@
 id: APR-SCWBS-043
 type: approval
 taskId: SCWBS-043
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:19:49.460Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:45:32.961Z"
+    approvalMode: human
+    headCommit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+    diffHash: sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
+    pullRequest: "#45"
+    reason: CONFIRM TTY APPROVAL SCWBS-043 post-finish 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0 sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:45:32.959Z"
+    verificationLevel: lean
 status: approved
-approvedBy: human
-approvedAt: "2026-07-02T18:40:00.794Z"
+requestedAt: "2026-08-24T19:19:49.460Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:45:32.961Z"
+approvalMode: human
+headCommit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+diffHash: sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
 pullRequest: "#45"
-reason: User delegated human completion review and accepted evidence
+reason: CONFIRM TTY APPROVAL SCWBS-043 post-finish 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0 sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:45:32.959Z"
+verificationLevel: lean
diff --git a/contracts/approvals/SCWBS-044.yaml b/contracts/approvals/SCWBS-044.yaml
index 9100434..9d9f38c 100644
--- a/contracts/approvals/SCWBS-044.yaml
+++ b/contracts/approvals/SCWBS-044.yaml
@@ -1,8 +1,33 @@
 id: APR-SCWBS-044
 type: approval
 taskId: SCWBS-044
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:19:58.172Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:45:45.668Z"
+    approvalMode: human
+    headCommit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+    diffHash: sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
+    pullRequest: "#46"
+    reason: CONFIRM TTY APPROVAL SCWBS-044 post-finish 33f7f918b0a1ef648ce917bde7111a27781ad1ff sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:45:45.667Z"
+    verificationLevel: lean
 status: approved
-approvedBy: human
-approvedAt: "2026-07-02T18:40:00.268Z"
+requestedAt: "2026-08-24T19:19:58.172Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:45:45.668Z"
+approvalMode: human
+headCommit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+diffHash: sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
 pullRequest: "#46"
-reason: User delegated human completion review and accepted evidence
+reason: CONFIRM TTY APPROVAL SCWBS-044 post-finish 33f7f918b0a1ef648ce917bde7111a27781ad1ff sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:45:45.667Z"
+verificationLevel: lean
diff --git a/contracts/approvals/SCWBS-045.yaml b/contracts/approvals/SCWBS-045.yaml
index 0760f15..b255f56 100644
--- a/contracts/approvals/SCWBS-045.yaml
+++ b/contracts/approvals/SCWBS-045.yaml
@@ -1,8 +1,33 @@
 id: APR-SCWBS-045
 type: approval
 taskId: SCWBS-045
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:20:07.063Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:45:57.979Z"
+    approvalMode: human
+    headCommit: d541daa873978f88e8d693f43e64f41a535f4c38
+    diffHash: sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
+    pullRequest: "#47"
+    reason: CONFIRM TTY APPROVAL SCWBS-045 post-finish d541daa873978f88e8d693f43e64f41a535f4c38 sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:45:57.978Z"
+    verificationLevel: lean
 status: approved
-approvedBy: human
-approvedAt: "2026-07-02T18:40:00.911Z"
+requestedAt: "2026-08-24T19:20:07.063Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:45:57.979Z"
+approvalMode: human
+headCommit: d541daa873978f88e8d693f43e64f41a535f4c38
+diffHash: sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
 pullRequest: "#47"
-reason: User delegated human completion review and accepted evidence
+reason: CONFIRM TTY APPROVAL SCWBS-045 post-finish d541daa873978f88e8d693f43e64f41a535f4c38 sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:45:57.978Z"
+verificationLevel: lean
diff --git a/contracts/approvals/SCWBS-046.yaml b/contracts/approvals/SCWBS-046.yaml
index dab1bef..17b7269 100644
--- a/contracts/approvals/SCWBS-046.yaml
+++ b/contracts/approvals/SCWBS-046.yaml
@@ -1,8 +1,33 @@
 id: APR-SCWBS-046
 type: approval
 taskId: SCWBS-046
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:20:16.033Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:46:28.611Z"
+    approvalMode: human
+    headCommit: aad4134dd7d49c55cc86aface971884cc443c7cb
+    diffHash: sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
+    pullRequest: "#48"
+    reason: CONFIRM TTY APPROVAL SCWBS-046 post-finish aad4134dd7d49c55cc86aface971884cc443c7cb sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:46:28.610Z"
+    verificationLevel: lean
 status: approved
-approvedBy: human
-approvedAt: "2026-07-02T18:40:01.545Z"
+requestedAt: "2026-08-24T19:20:16.033Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:46:28.611Z"
+approvalMode: human
+headCommit: aad4134dd7d49c55cc86aface971884cc443c7cb
+diffHash: sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
 pullRequest: "#48"
-reason: User delegated human completion review and accepted evidence
+reason: CONFIRM TTY APPROVAL SCWBS-046 post-finish aad4134dd7d49c55cc86aface971884cc443c7cb sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:46:28.610Z"
+verificationLevel: lean
diff --git a/contracts/approvals/SCWBS-047.yaml b/contracts/approvals/SCWBS-047.yaml
new file mode 100644
index 0000000..6852f7a
--- /dev/null
+++ b/contracts/approvals/SCWBS-047.yaml
@@ -0,0 +1,33 @@
+id: APR-SCWBS-047
+type: approval
+taskId: SCWBS-047
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:20:25.137Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:47:10.175Z"
+    approvalMode: human
+    headCommit: 557390043386bbcaa92e99495b7ab3337827d6bf
+    diffHash: sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
+    pullRequest: "#49"
+    reason: CONFIRM TTY APPROVAL SCWBS-047 post-finish 557390043386bbcaa92e99495b7ab3337827d6bf sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:47:10.174Z"
+    verificationLevel: lean
+status: approved
+requestedAt: "2026-08-24T19:20:25.137Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:47:10.175Z"
+approvalMode: human
+headCommit: 557390043386bbcaa92e99495b7ab3337827d6bf
+diffHash: sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
+pullRequest: "#49"
+reason: CONFIRM TTY APPROVAL SCWBS-047 post-finish 557390043386bbcaa92e99495b7ab3337827d6bf sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:47:10.174Z"
+verificationLevel: lean
diff --git a/contracts/approvals/SCWBS-048.yaml b/contracts/approvals/SCWBS-048.yaml
new file mode 100644
index 0000000..75b5da6
--- /dev/null
+++ b/contracts/approvals/SCWBS-048.yaml
@@ -0,0 +1,33 @@
+id: APR-SCWBS-048
+type: approval
+taskId: SCWBS-048
+version: scwbs.approval.v2
+activeScope: post-finish
+scopeApprovals:
+  post-finish:
+    status: approved
+    requestedAt: "2026-08-24T19:20:34.140Z"
+    approvedBy: x_meta
+    approvedAt: "2026-08-24T19:47:21.001Z"
+    approvalMode: human
+    headCommit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
+    diffHash: sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
+    pullRequest: "#50"
+    reason: CONFIRM TTY APPROVAL SCWBS-048 post-finish ee504c805f41276bb1f2981d43b4a39db4a0f4c9 sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
+    actorId: x_meta
+    actorSource: tty
+    verifiedAt: "2026-08-24T19:47:21.000Z"
+    verificationLevel: lean
+status: approved
+requestedAt: "2026-08-24T19:20:34.140Z"
+approvedBy: x_meta
+approvedAt: "2026-08-24T19:47:21.001Z"
+approvalMode: human
+headCommit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
+diffHash: sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
+pullRequest: "#50"
+reason: CONFIRM TTY APPROVAL SCWBS-048 post-finish ee504c805f41276bb1f2981d43b4a39db4a0f4c9 sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
+actorId: x_meta
+actorSource: tty
+verifiedAt: "2026-08-24T19:47:21.000Z"
+verificationLevel: lean
diff --git a/contracts/blocks/SCWBS-DRAFT-MT7ICSMN.yaml b/contracts/blocks/SCWBS-DRAFT-MT7ICSMN.yaml
new file mode 100644
index 0000000..6cd43d6
--- /dev/null
+++ b/contracts/blocks/SCWBS-DRAFT-MT7ICSMN.yaml
@@ -0,0 +1,21 @@
+id: BLK-SCWBS-DRAFT-MT7ICSMN
+type: block
+taskId: SCWBS-DRAFT-MT7ICSMN
+status: resolved
+category: human-gate
+level: 1
+requiredHumanDecision: Complete the required human gate decision before implementation continues.
+reason: Human Gate required before atomically rewriting SCWBS-041 through SCWBS-048 Evidence subjects and lifecycle artifacts
+createdAt: "2026-08-24T17:45:54.712Z"
+history:
+  - status: blocked
+    at: "2026-08-24T17:45:54.712Z"
+    reason: Human Gate required before atomically rewriting SCWBS-041 through SCWBS-048 Evidence subjects and lifecycle artifacts
+    by: ai-agent
+  - status: resolved
+    at: "2026-08-24T17:57:34.594Z"
+    reason: Approve the atomic migration of SCWBS-041 through SCWBS-048 Evidence subjects and lifecycle artifacts within the locked exact-path Task Contract
+    by: human
+resolvedAt: "2026-08-24T17:57:34.594Z"
+resolvedBy: human
+resolution: Approve the atomic migration of SCWBS-041 through SCWBS-048 Evidence subjects and lifecycle artifacts within the locked exact-path Task Contract
diff --git a/contracts/evidence-payloads/SCWBS-041.patch b/contracts/evidence-payloads/SCWBS-041.patch
new file mode 100644
index 0000000..0857bfe
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-041.patch
@@ -0,0 +1,645 @@
+diff --git a/contracts/spec-changes/SCP-SCWBS-041.yaml b/contracts/spec-changes/SCP-SCWBS-041.yaml
+new file mode 100644
+index 0000000..62d1e35
+--- /dev/null
++++ b/contracts/spec-changes/SCP-SCWBS-041.yaml
+@@ -0,0 +1,26 @@
++id: SCP-SCWBS-041
++type: spec-change-proposal
++status: approved
++targetSpec: SPEC-SCWBS-METHOD
++currentVersion: 0.1.0
++proposedVersion: 0.1.1
++taskId: SCWBS-041
++level: 2
++summary: Add Spec Change Proposal as a first-class SC-WBS contract artifact.
++rationale:
++  - Level 2 specification changes need a canonical proposal artifact before approved specs are updated.
++  - The design resolution plan selected Spec Change Proposal as the next missing control artifact.
++affectedPaths:
++  - src/core/types.ts
++  - src/core/schema.ts
++  - src/core/contracts.ts
++  - src/commands/check.ts
++  - src/commands/registry-rebuild.ts
++  - docs/scwbs/operations-profile-and-specs.md
++approval:
++  required: true
++  status: approved
++risks:
++  - Future tasks still need a command-level workflow for creating proposals.
++approvedBy: human
++approvedAt: "2026-07-02T00:00:00+09:00"
+diff --git a/contracts/tasks/SCWBS-041.yaml b/contracts/tasks/SCWBS-041.yaml
+new file mode 100644
+index 0000000..7a5a9ab
+--- /dev/null
++++ b/contracts/tasks/SCWBS-041.yaml
+@@ -0,0 +1,36 @@
++id: SCWBS-041
++type: task-contract
++wbsNodeId: node-governance-maintenance
++featureId: F-SCWBS-GOVERNANCE
++branchName: codex/scwbs-041-spec-change-proposal
++allowedPaths:
++  - src/core/**
++  - src/commands/registry-rebuild.ts
++  - src/commands/check.ts
++  - tests/**
++  - docs/scwbs/operations-profile-and-specs.md
++  - docs/implementation-gaps.md
++  - contracts/spec-changes/**
++  - contracts/tasks/SCWBS-041.yaml
++  - contracts/evidence/SCWBS-041.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths:
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - Spec Change Proposal files are first-class contract artifacts under contracts/spec-changes.
++  - scwbs check validates Spec Change Proposal schema and required approval metadata.
++  - registry rebuild indexes Spec Change Proposal files.
++  - Documentation describes the artifact and its minimal workflow.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T00:00:00.000Z"
+diff --git a/docs/implementation-gaps.md b/docs/implementation-gaps.md
+index 049ecc0..88f9085 100644
+--- a/docs/implementation-gaps.md
++++ b/docs/implementation-gaps.md
+@@ -13,6 +13,7 @@ This document tracks the pieces that are intentionally still missing from the cu
+ - Optional Contract Lock metadata validation
+ - Task Contract Lock generation from WBS and Spec content hashes
+ - First-class Spec Contract files under `contracts/specs/*.yaml` with required metadata validation
++- First-class Spec Change Proposal files under `contracts/spec-changes/*.yaml` with required metadata validation
+ - Evidence `testQuality` metadata validation
+ - Base/head-aware Evidence changed file collection
+ - AI blocked-task change-set generation
+@@ -30,7 +31,7 @@ This document tracks the pieces that are intentionally still missing from the cu
+ |---|---|---|
+ | Contract freshness | Lock refresh policy and commands | Approved Spec Contract artifacts now exist, but the CLI does not yet define when stale locks may be refreshed |
+ | Contract locking | Stale lock refresh flow | The CLI can create lock metadata, but it does not yet define when stale locks may be refreshed |
+-| Change control | Spec Change Proposal format and command | Spec gaps still rely on ad hoc human coordination |
++| Change control | Spec Change Proposal command and workflow enforcement | Spec Change Proposal files exist, but creation and Level 2 gating are not yet automated |
+ | Risk management | Risk Register format and command | Strict workflows still need a formal risk log |
+ | Evidence trust | CI artifact verification and stronger provenance checks | Evidence is still mostly heuristic and metadata-driven |
+ | Evidence diff basis | Richer PR metadata and CI correlation | `evidence collect` records branch-diff provenance, but PR number and CI run correlation are still optional metadata |
+@@ -46,7 +47,7 @@ This document tracks the pieces that are intentionally still missing from the cu
+ ## Near-Term Follow-Ups
+ 
+ - Define a lock refresh policy for stale Task Contracts.
+-- Introduce a lightweight spec-change proposal artifact.
++- Add a command to create lightweight spec-change proposal artifacts.
+ - Parse test diffs or coverage summaries instead of relying only on `testQuality` metadata. Prefer AST-based assertion counting where practical, and coverage-report comparison as the lower-cost first step.
+ - Make `ai next-task` priority-aware.
+ - Implement a low-cost WBS semantic merge helper before full distributed WBS support.
+diff --git a/docs/scwbs/operations-profile-and-specs.md b/docs/scwbs/operations-profile-and-specs.md
+index 73f1a61..93ca05a 100644
+--- a/docs/scwbs/operations-profile-and-specs.md
++++ b/docs/scwbs/operations-profile-and-specs.md
+@@ -38,8 +38,6 @@ Human Gateで責任ある判断を制御する。
+ 
+ 次段階の正式候補は以下である。
+ 
+-* Spec Contractに `status`、`version`、`approvedBy`、`approvedAt` を持たせる
+-* Spec Change Proposalの形式を定義する
+ * Strict Profile向けにRisk Registerの形式を定義する
+ 
+ ## 17. Subtree Phase
+@@ -58,6 +56,46 @@ subtreeのphaseは `nodes[].extensions.scwbs.phase` に記録する。
+ Spec Contract files live under `contracts/specs/*.yaml`.
+ Approved Spec Contracts must include `status`, `version`, `approvedBy`, and `approvedAt`.
+ 
++## 19. Spec Change Proposal Files
++
++Spec Change Proposal files live under `contracts/spec-changes/*.yaml`.
++They describe proposed Level 1 or Level 2 changes before the approved Spec Contract is updated.
++
++Minimum form:
++
++```yaml
++id: SCP-SCWBS-001
++type: spec-change-proposal
++status: proposed
++targetSpec: SPEC-SCWBS-METHOD
++currentVersion: 0.1.0
++proposedVersion: 0.2.0
++taskId: SCWBS-041
++level: 2
++summary: Define a new contract artifact.
++rationale:
++  - The current approved spec does not define the changed behavior.
++affectedPaths:
++  - docs/scwbs/operations-profile-and-specs.md
++approval:
++  required: true
++  status: requested
++risks:
++  - Existing task locks may need refresh after approval.
++```
++
++`status` is one of:
++
++| Status | Meaning |
++|---|---|
++| `proposed` | Proposed and not yet approved |
++| `approved` | Human-approved and ready to drive Spec Contract updates |
++| `rejected` | Not accepted |
++| `superseded` | Replaced by another proposal |
++
++When `status: approved`, `approvedBy` and `approvedAt` are required.
++`scwbs check` validates Spec Change Proposal files and requires them to be indexed by `contracts/registry.yaml`.
++
+ ### Approval Record 補足
+ 
+ Human approval record は `contracts/approvals/*.yaml` に置く。
+diff --git a/src/commands/check.ts b/src/commands/check.ts
+index 591b817..873add0 100644
+--- a/src/commands/check.ts
++++ b/src/commands/check.ts
+@@ -1,5 +1,5 @@
+ import { existsSync } from "node:fs";
+-import { approvalExists, evidenceExists, listApprovals, listSpecs, listTasks, matchingRegistrySpecByPath, readEvidence, readRegistry, readSpecFromRegistryContract, resolveSpecForTask } from "../core/contracts.js";
++import { approvalExists, evidenceExists, listApprovals, listSpecChanges, listSpecs, listTasks, matchingRegistrySpecByPath, matchingRegistrySpecChangeByPath, readEvidence, readRegistry, readSpecFromRegistryContract, resolveSpecForTask } from "../core/contracts.js";
+ import { workingTreeChangedFiles } from "../core/git.js";
+ import { fileSha256 } from "../core/hash.js";
+ import { defaultWbsPath, resolveFrom } from "../core/paths.js";
+@@ -146,6 +146,23 @@ function validateIndexedSpecs(root: string, registry: Registry | undefined): Iss
+   return issues;
+ }
+ 
++function validateIndexedSpecChanges(root: string, registry: Registry | undefined): Issue[] {
++  const issues: Issue[] = [];
++  for (const entry of listSpecChanges(root)) {
++    issues.push(...entry.issues);
++    if (!entry.specChange) continue;
++    const registryContract = matchingRegistrySpecChangeByPath(registry, entry.path);
++    if (!registryContract) {
++      issues.push({
++        severity: "error",
++        code: "specChange.registry.missing",
++        message: `${entry.path} is not indexed by contracts/registry.yaml`
++      });
++    }
++  }
++  return issues;
++}
++
+ export function collectWbsChangesetGateIssues(files: string[]): Issue[] {
+   const issues: Issue[] = [];
+   const changesWbs = files.some((file) => file.replace(/\\/g, "/") === "contracts/wbs/project.wbs.json");
+@@ -178,6 +195,7 @@ export function collectCheckIssues(root: string): Issue[] {
+   issues.push(...registryIssues);
+   issues.push(...validateRegistryContracts(root, registry));
+   issues.push(...validateIndexedSpecs(root, registry));
++  issues.push(...validateIndexedSpecChanges(root, registry));
+   for (const entry of listApprovals(root)) {
+     issues.push(...entry.issues);
+   }
+diff --git a/src/commands/registry-rebuild.ts b/src/commands/registry-rebuild.ts
+index 35364cc..c880302 100644
+--- a/src/commands/registry-rebuild.ts
++++ b/src/commands/registry-rebuild.ts
+@@ -1,5 +1,5 @@
+ import { existsSync, readFileSync, writeFileSync } from "node:fs";
+-import { listApprovals, listEvidence, listReviews, listSpecs, listTasks } from "../core/contracts.js";
++import { listApprovals, listEvidence, listReviews, listSpecChanges, listSpecs, listTasks } from "../core/contracts.js";
+ import { defaultRegistryPath, resolveFrom } from "../core/paths.js";
+ import { stringifySimpleYaml } from "../core/yaml.js";
+ import { readWbs } from "../core/wbs.js";
+@@ -11,6 +11,10 @@ export function buildRegistryYaml(root: string): string {
+     if (!spec) continue;
+     contracts.push({ id: spec.id, type: "spec", path, status: spec.status, version: spec.version, featureId: spec.featureId });
+   }
++  for (const { specChange, path } of listSpecChanges(root)) {
++    if (!specChange) continue;
++    contracts.push({ id: specChange.id, type: "spec-change", path, status: specChange.status, version: specChange.proposedVersion, relatedTask: specChange.taskId });
++  }
+   for (const { task, path } of listTasks(root)) {
+     if (!task) continue;
+     contracts.push({ id: `TASK-${task.id}`, type: "task", path, featureId: task.featureId });
+diff --git a/src/core/contracts.ts b/src/core/contracts.ts
+index 805a3c0..0b28141 100644
+--- a/src/core/contracts.ts
++++ b/src/core/contracts.ts
+@@ -1,8 +1,8 @@
+ import { existsSync, readdirSync } from "node:fs";
+ import { readYamlFile } from "./yaml.js";
+-import { approvalPath, defaultApprovalsDir, defaultEvidenceDir, defaultRegistryPath, defaultReviewsDir, defaultSpecsDir, defaultTasksDir, resolveFrom, reviewPath, taskPath, evidencePath } from "./paths.js";
+-import { asApprovalRecord, asEvidence, asRegistry, asReviewRecord, asSpecContract, asTaskContract, validateApprovalRecord, validateApprovalRecordSchema, validateEvidence, validateEvidenceSchema, validateRegistry, validateRegistrySchema, validateReviewRecord, validateReviewRecordSchema, validateSpecContract, validateSpecContractSchema, validateTaskContract, validateTaskContractSchema } from "./schema.js";
+-import type { ApprovalRecord, Evidence, Issue, Registry, RegistryContract, ReviewRecord, SpecContract, TaskContract } from "./types.js";
++import { approvalPath, defaultApprovalsDir, defaultEvidenceDir, defaultRegistryPath, defaultReviewsDir, defaultSpecChangesDir, defaultSpecsDir, defaultTasksDir, resolveFrom, reviewPath, taskPath, evidencePath } from "./paths.js";
++import { asApprovalRecord, asEvidence, asRegistry, asReviewRecord, asSpecChangeProposal, asSpecContract, asTaskContract, validateApprovalRecord, validateApprovalRecordSchema, validateEvidence, validateEvidenceSchema, validateRegistry, validateRegistrySchema, validateReviewRecord, validateReviewRecordSchema, validateSpecChangeProposal, validateSpecChangeProposalSchema, validateSpecContract, validateSpecContractSchema, validateTaskContract, validateTaskContractSchema } from "./schema.js";
++import type { ApprovalRecord, Evidence, Issue, Registry, RegistryContract, ReviewRecord, SpecChangeProposal, SpecContract, TaskContract } from "./types.js";
+ 
+ export function readRegistry(root: string): { registry?: Registry; issues: Issue[] } {
+   const fullPath = resolveFrom(root, defaultRegistryPath);
+@@ -35,6 +35,16 @@ export function readSpec(root: string, relativePath: string): { spec?: SpecContr
+   return { spec: issues.length === 0 ? asSpecContract(value) : undefined, issues };
+ }
+ 
++export function readSpecChange(root: string, relativePath: string): { specChange?: SpecChangeProposal; issues: Issue[] } {
++  const fullPath = resolveFrom(root, relativePath);
++  if (!existsSync(fullPath)) {
++    return { issues: [{ severity: "error", code: "specChange.missing", message: `${relativePath} does not exist` }] };
++  }
++  const value = readYamlFile<unknown>(fullPath);
++  const issues = [...validateSpecChangeProposalSchema(value, relativePath), ...validateSpecChangeProposal(value, relativePath)];
++  return { specChange: issues.length === 0 ? asSpecChangeProposal(value) : undefined, issues };
++}
++
+ export function readEvidence(root: string, taskId: string): { evidence?: Evidence; issues: Issue[] } {
+   const relativePath = evidencePath(taskId);
+   const fullPath = resolveFrom(root, relativePath);
+@@ -94,6 +104,19 @@ export function listSpecs(root: string): Array<{ spec?: SpecContract; issues: Is
+     });
+ }
+ 
++export function listSpecChanges(root: string): Array<{ specChange?: SpecChangeProposal; issues: Issue[]; path: string }> {
++  const dir = resolveFrom(root, defaultSpecChangesDir);
++  if (!existsSync(dir)) return [];
++  return readdirSync(dir)
++    .filter((file) => file.endsWith(".yaml") || file.endsWith(".yml"))
++    .map((file) => {
++      const path = `${defaultSpecChangesDir}/${file}`;
++      const value = readYamlFile<unknown>(resolveFrom(root, path));
++      const issues = [...validateSpecChangeProposalSchema(value, path), ...validateSpecChangeProposal(value, path)];
++      return { specChange: issues.length === 0 ? asSpecChangeProposal(value) : undefined, issues, path };
++    });
++}
++
+ export function listApprovals(root: string): Array<{ approval?: ApprovalRecord; issues: Issue[]; path: string }> {
+   const dir = resolveFrom(root, defaultApprovalsDir);
+   if (!existsSync(dir)) return [];
+@@ -144,6 +167,10 @@ export function matchingRegistrySpecByPath(registry: Registry | undefined, specP
+   return registry?.contracts.find((contract) => contract.type === "spec" && contract.path === specPath);
+ }
+ 
++export function matchingRegistrySpecChangeByPath(registry: Registry | undefined, specChangePath: string): RegistryContract | undefined {
++  return registry?.contracts.find((contract) => contract.type === "spec-change" && contract.path === specChangePath);
++}
++
+ export function readSpecFromRegistryContract(root: string, contract: RegistryContract): { spec?: SpecContract; path: string; issues: Issue[] } {
+   const { spec, issues } = readSpec(root, contract.path);
+   return { spec, path: contract.path, issues };
+diff --git a/src/core/paths.ts b/src/core/paths.ts
+index b5111df..95f35d2 100644
+--- a/src/core/paths.ts
++++ b/src/core/paths.ts
+@@ -3,6 +3,7 @@ import path from "node:path";
+ export const defaultWbsPath = "contracts/wbs/project.wbs.json";
+ export const defaultRegistryPath = "contracts/registry.yaml";
+ export const defaultSpecsDir = "contracts/specs";
++export const defaultSpecChangesDir = "contracts/spec-changes";
+ export const defaultTasksDir = "contracts/tasks";
+ export const defaultEvidenceDir = "contracts/evidence";
+ export const defaultApprovalsDir = "contracts/approvals";
+@@ -20,6 +21,10 @@ export function specPath(specId: string): string {
+   return `${defaultSpecsDir}/${specId}.yaml`;
+ }
+ 
++export function specChangePath(specChangeId: string): string {
++  return `${defaultSpecChangesDir}/${specChangeId}.yaml`;
++}
++
+ export function evidencePath(taskId: string): string {
+   return `${defaultEvidenceDir}/${taskId}.yaml`;
+ }
+diff --git a/src/core/schema.ts b/src/core/schema.ts
+index e82579f..dd13b33 100644
+--- a/src/core/schema.ts
++++ b/src/core/schema.ts
+@@ -1,5 +1,5 @@
+ import { Ajv, type ErrorObject } from "ajv";
+-import type { ApprovalRecord, Evidence, Issue, Registry, ReviewRecord, SpecContract, TaskContract, WbsDocument } from "./types.js";
++import type { ApprovalRecord, Evidence, Issue, Registry, ReviewRecord, SpecChangeProposal, SpecContract, TaskContract, WbsDocument } from "./types.js";
+ 
+ function isObject(value: unknown): value is Record<string, unknown> {
+   return typeof value === "object" && value !== null && !Array.isArray(value);
+@@ -34,7 +34,7 @@ const registrySchema = {
+         additionalProperties: true,
+         properties: {
+           id: { type: "string", minLength: 1 },
+-          type: { type: "string", enum: ["requirement", "spec", "task", "evidence", "approval", "review", "adr"] },
++          type: { type: "string", enum: ["requirement", "spec", "spec-change", "task", "evidence", "approval", "review", "adr"] },
+           path: { type: "string", minLength: 1 },
+           status: { type: "string" },
+           version: { type: "string" },
+@@ -65,6 +65,36 @@ const specContractSchema = {
+   }
+ };
+ 
++const specChangeProposalSchema = {
++  type: "object",
++  required: ["id", "type", "status", "targetSpec", "currentVersion", "proposedVersion", "taskId", "level", "summary", "rationale", "affectedPaths"],
++  additionalProperties: true,
++  properties: {
++    id: { type: "string", minLength: 1 },
++    type: { const: "spec-change-proposal" },
++    status: { type: "string", enum: ["proposed", "approved", "rejected", "superseded"] },
++    targetSpec: { type: "string", minLength: 1 },
++    currentVersion: { type: "string", minLength: 1 },
++    proposedVersion: { type: "string", minLength: 1 },
++    taskId: { type: "string", minLength: 1 },
++    level: { type: "integer", enum: [0, 1, 2] },
++    summary: { type: "string", minLength: 1 },
++    rationale: stringArraySchema,
++    affectedPaths: stringArraySchema,
++    approval: {
++      type: "object",
++      additionalProperties: true,
++      properties: {
++        required: { type: "boolean" },
++        status: { type: "string", enum: ["requested", "approved", "rejected"] }
++      }
++    },
++    risks: stringArraySchema,
++    approvedBy: { type: "string" },
++    approvedAt: { type: "string" }
++  }
++};
++
+ const taskContractSchema = {
+   type: "object",
+   required: ["id", "type", "wbsNodeId", "featureId", "allowedPaths", "forbiddenPaths", "humanGateRequiredPaths", "requiredChecks", "doneCriteria", "evidenceRequired"],
+@@ -200,6 +230,7 @@ const reviewRecordSchema = {
+ const schemaValidators = {
+   registry: ajv.compile(registrySchema),
+   spec: ajv.compile(specContractSchema),
++  specChange: ajv.compile(specChangeProposalSchema),
+   task: ajv.compile(taskContractSchema),
+   evidence: ajv.compile(evidenceSchema),
+   approval: ajv.compile(approvalRecordSchema),
+@@ -224,6 +255,10 @@ export function validateSpecContractSchema(value: unknown, filePath = "spec"): I
+   return schemaIssues("spec", value, filePath);
+ }
+ 
++export function validateSpecChangeProposalSchema(value: unknown, filePath = "spec-change"): Issue[] {
++  return schemaIssues("specChange", value, filePath);
++}
++
+ export function validateTaskContractSchema(value: unknown, filePath = "task"): Issue[] {
+   return schemaIssues("task", value, filePath);
+ }
+@@ -308,6 +343,64 @@ export function asSpecContract(value: unknown): SpecContract {
+   return value as SpecContract;
+ }
+ 
++export function validateSpecChangeProposal(value: unknown, filePath = "spec-change"): Issue[] {
++  const issues: Issue[] = [];
++  if (!isObject(value)) return [issue("specChange.invalid", `${filePath} must be an object`)];
++
++  for (const key of ["id", "type", "status", "targetSpec", "currentVersion", "proposedVersion", "taskId", "summary"]) {
++    if (typeof value[key] !== "string" || value[key].length === 0) {
++      issues.push(issue("specChange.field", `${filePath}.${key} must be a non-empty string`));
++    }
++  }
++  if (value.type !== "spec-change-proposal") {
++    issues.push(issue("specChange.type", `${filePath}.type must be spec-change-proposal`));
++  }
++  if (value.status !== undefined && !["proposed", "approved", "rejected", "superseded"].includes(String(value.status))) {
++    issues.push(issue("specChange.status", `${filePath}.status must be proposed, approved, rejected, or superseded`));
++  }
++  if (value.level !== 0 && value.level !== 1 && value.level !== 2) {
++    issues.push(issue("specChange.level", `${filePath}.level must be 0, 1, or 2`));
++  }
++  if (!isStringArray(value.rationale)) {
++    issues.push(issue("specChange.rationale", `${filePath}.rationale must be a string array`));
++  }
++  if (!isStringArray(value.affectedPaths)) {
++    issues.push(issue("specChange.affectedPaths", `${filePath}.affectedPaths must be a string array`));
++  }
++  if (value.risks !== undefined && !isStringArray(value.risks)) {
++    issues.push(issue("specChange.risks", `${filePath}.risks must be a string array when present`));
++  }
++  if (value.approval !== undefined) {
++    if (!isObject(value.approval)) {
++      issues.push(issue("specChange.approval", `${filePath}.approval must be an object when present`));
++    } else {
++      if (value.approval.required !== undefined && typeof value.approval.required !== "boolean") {
++        issues.push(issue("specChange.approval", `${filePath}.approval.required must be a boolean when present`));
++      }
++      if (value.approval.status !== undefined && !["requested", "approved", "rejected"].includes(String(value.approval.status))) {
++        issues.push(issue("specChange.approval", `${filePath}.approval.status must be requested, approved, or rejected`));
++      }
++    }
++  }
++  for (const key of ["approvedBy", "approvedAt"]) {
++    if (value[key] !== undefined && typeof value[key] !== "string") {
++      issues.push(issue("specChange.field", `${filePath}.${key} must be a string when present`));
++    }
++  }
++  if (value.status === "approved") {
++    for (const key of ["approvedBy", "approvedAt"]) {
++      if (typeof value[key] !== "string" || value[key].length === 0) {
++        issues.push(issue("specChange.approval", `${filePath}.${key} must be present when status is approved`));
++      }
++    }
++  }
++  return issues;
++}
++
++export function asSpecChangeProposal(value: unknown): SpecChangeProposal {
++  return value as SpecChangeProposal;
++}
++
+ export function validateTaskContract(value: unknown, filePath = "task"): Issue[] {
+   const issues: Issue[] = [];
+   if (!isObject(value)) return [issue("task.invalid", `${filePath} must be an object`)];
+diff --git a/src/core/types.ts b/src/core/types.ts
+index 5bb8dc7..2fe4904 100644
+--- a/src/core/types.ts
++++ b/src/core/types.ts
+@@ -6,7 +6,7 @@ export type Issue = {
+   message: string;
+ };
+ 
+-export type RegistryContractType = "requirement" | "spec" | "task" | "evidence" | "approval" | "review" | "adr";
++export type RegistryContractType = "requirement" | "spec" | "spec-change" | "task" | "evidence" | "approval" | "review" | "adr";
+ 
+ export type RegistryContract = {
+   id: string;
+@@ -26,6 +26,7 @@ export type Registry = {
+ export type ApprovalStatus = "requested" | "approved" | "rejected";
+ 
+ export type SpecContractStatus = "draft" | "approved" | "superseded";
++export type SpecChangeProposalStatus = "proposed" | "approved" | "rejected" | "superseded";
+ 
+ export type SpecContract = {
+   id: string;
+@@ -41,6 +42,27 @@ export type SpecContract = {
+   approvedAt?: string;
+ };
+ 
++export type SpecChangeProposal = {
++  id: string;
++  type: "spec-change-proposal";
++  status: SpecChangeProposalStatus;
++  targetSpec: string;
++  currentVersion: string;
++  proposedVersion: string;
++  taskId: string;
++  level: 0 | 1 | 2;
++  summary: string;
++  rationale: string[];
++  affectedPaths: string[];
++  approval?: {
++    required?: boolean;
++    status?: ApprovalStatus;
++  };
++  risks?: string[];
++  approvedBy?: string;
++  approvedAt?: string;
++};
++
+ export type TaskContract = {
+   id: string;
+   type: "task-contract";
+diff --git a/tests/helpers.ts b/tests/helpers.ts
+index 7f91e97..a0673e1 100644
+--- a/tests/helpers.ts
++++ b/tests/helpers.ts
+@@ -2,7 +2,7 @@ import { mkdirSync, writeFileSync } from "node:fs";
+ import { tmpdir } from "node:os";
+ import path from "node:path";
+ import { execFileSync } from "node:child_process";
+-import type { ApprovalRecord, Evidence, SpecContract, TaskContract, WbsDocument } from "../src/core/types.js";
++import type { ApprovalRecord, Evidence, SpecChangeProposal, SpecContract, TaskContract, WbsDocument } from "../src/core/types.js";
+ import { stringifySimpleYaml } from "../src/core/yaml.js";
+ 
+ export function makeTempRepo(): string {
+@@ -110,6 +110,28 @@ export function sampleSpec(overrides: Partial<SpecContract> = {}): SpecContract
+   };
+ }
+ 
++export function sampleSpecChange(overrides: Partial<SpecChangeProposal> = {}): SpecChangeProposal {
++  return {
++    id: "SCP-F001-API-001",
++    type: "spec-change-proposal",
++    status: "proposed",
++    targetSpec: "SPEC-F001-API",
++    currentVersion: "1.0.0",
++    proposedVersion: "1.1.0",
++    taskId: "WBS-001-004",
++    level: 2,
++    summary: "Propose an API spec change.",
++    rationale: ["The current spec does not define the changed behavior."],
++    affectedPaths: ["contracts/specs/SPEC-F001-API.yaml"],
++    approval: {
++      required: true,
++      status: "requested"
++    },
++    risks: ["Existing tasks may need lock refresh."],
++    ...overrides
++  };
++}
++
+ export function sampleEvidence(overrides: Partial<Evidence> = {}): Evidence {
+   return {
+     id: "EVD-001-004",
+diff --git a/tests/scwbs.test.ts b/tests/scwbs.test.ts
+index 4f4c5ed..7bc0894 100644
+--- a/tests/scwbs.test.ts
++++ b/tests/scwbs.test.ts
+@@ -23,11 +23,11 @@ import { buildNextAction } from "../src/commands/next.js";
+ import { buildDraftTaskYaml, runTaskGenerate } from "../src/commands/task-generate.js";
+ import { buildLockedTask, runTaskLock } from "../src/commands/task-lock.js";
+ import { runWbsValidate, runWbsApply } from "../src/commands/wbs.js";
+-import { listSpecs, readApproval, readEvidence, readRegistry, readReview, readSpec, readTask } from "../src/core/contracts.js";
++import { listSpecChanges, listSpecs, readApproval, readEvidence, readRegistry, readReview, readSpec, readSpecChange, readTask } from "../src/core/contracts.js";
+ import { baseBranchStatus, branchChangedFiles, filesAddedOnBothSides, headCommit, workingTreeChangedFiles } from "../src/core/git.js";
+ import { validateWbsDocument } from "../src/core/wbs.js";
+ import { main } from "../src/cli.js";
+-import { makeTempRepo, sampleApproval, sampleTask, sampleWbs, sampleSpec, writeJson, writeScwbsProject, writeText, writeYaml, sampleEvidence } from "./helpers.js";
++import { makeTempRepo, sampleApproval, sampleTask, sampleWbs, sampleSpec, sampleSpecChange, writeJson, writeScwbsProject, writeText, writeYaml, sampleEvidence } from "./helpers.js";
+ 
+ describe("scwbs MVP", () => {
+   test("init creates a valid minimal WJS document", () => {
+@@ -67,6 +67,47 @@ describe("scwbs MVP", () => {
+     expect(listSpecs(root).some((entry) => entry.path === "contracts/specs/SPEC-F001-API.yaml" && entry.issues.length === 0)).toBe(true);
+   });
+ 
++  test("spec change proposals are first-class files with required metadata", () => {
++    const root = makeTempRepo();
++    writeScwbsProject(root);
++    writeYaml(root, "contracts/spec-changes/SCP-F001-API-001.yaml", sampleSpecChange() as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/registry.yaml", {
++      projectId: "test-wbs",
++      contracts: [
++        {
++          id: "SPEC-F001-API",
++          type: "spec",
++          path: "contracts/specs/SPEC-F001-API.yaml",
++          status: "approved",
++          version: "1.0.0",
++          featureId: "F001",
++          relatedTask: "WBS-001-004"
++        },
++        {
++          id: "SCP-F001-API-001",
++          type: "spec-change",
++          path: "contracts/spec-changes/SCP-F001-API-001.yaml",
++          status: "proposed",
++          version: "1.1.0",
++          relatedTask: "WBS-001-004"
++        },
++        {
++          id: "TASK-WBS-001-004",
++          type: "task",
++          path: "contracts/tasks/WBS-001-004.yaml",
++          featureId: "F001"
++        }
++      ]
++    });
++
++    const { specChange, issues } = readSpecChange(root, "contracts/spec-changes/SCP-F001-API-001.yaml");
++    expect(issues).toEqual([]);
++    expect(specChange?.type).toBe("spec-change-proposal");
++    expect(specChange?.status).toBe("proposed");
++    expect(listSpecChanges(root).some((entry) => entry.path === "contracts/spec-changes/SCP-F001-API-001.yaml" && entry.issues.length === 0)).toBe(true);
++    expect(collectCheckIssues(root).some((issue) => issue.code.startsWith("specChange."))).toBe(false);
++  });
++
+   test("YAML contract reads run JSON Schema validation after parsing", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root);
+@@ -85,6 +126,7 @@ describe("scwbs MVP", () => {
+     writeScwbsProject(root);
+     writeYaml(root, "contracts/registry.yaml", { projectId: "test-wbs", contracts: [{ id: "TASK-WBS-001-004", type: "bad", path: "contracts/tasks/WBS-001-004.yaml" }] });
+     writeYaml(root, "contracts/specs/SPEC-F001-API.yaml", { ...sampleSpec(), status: "archived" });
++    writeYaml(root, "contracts/spec-changes/SCP-F001-API-001.yaml", { ...sampleSpecChange(), status: "waiting" });
+     writeYaml(root, "contracts/evidence/WBS-001-004.yaml", { ...sampleEvidence(), checks: [{ name: "test", status: "unknown" }] });
+     writeYaml(root, "contracts/approvals/WBS-001-004.yaml", { ...sampleApproval(), status: "waiting" });
+     writeYaml(root, "contracts/reviews/WBS-001-004.yaml", {
+@@ -98,6 +140,7 @@ describe("scwbs MVP", () => {
+ 
+     expect(readRegistry(root).issues.some((issue) => issue.code === "registry.schema")).toBe(true);
+     expect(readSpec(root, "contracts/specs/SPEC-F001-API.yaml").issues.some((issue) => issue.code === "spec.schema")).toBe(true);
++    expect(readSpecChange(root, "contracts/spec-changes/SCP-F001-API-001.yaml").issues.some((issue) => issue.code === "specChange.schema")).toBe(true);
+     expect(readEvidence(root, "WBS-001-004").issues.some((issue) => issue.code === "evidence.schema")).toBe(true);
+     expect(readApproval(root, "WBS-001-004").issues.some((issue) => issue.code === "approval.schema")).toBe(true);
+     expect(readReview(root, "WBS-001-004").issues.some((issue) => issue.code === "review.schema")).toBe(true);
+@@ -982,6 +1025,14 @@ describe("scwbs MVP", () => {
+     expect(issues.some((issue) => issue.code === "spec.registry.missing")).toBe(true);
+   });
+ 
++  test("check errors when a spec change file is not indexed in the registry", () => {
++    const root = makeTempRepo();
++    writeScwbsProject(root);
++    writeYaml(root, "contracts/spec-changes/SCP-F001-API-001.yaml", sampleSpecChange() as unknown as Record<string, unknown>);
++    const issues = collectCheckIssues(root);
++    expect(issues.some((issue) => issue.code === "specChange.registry.missing")).toBe(true);
++  });
++
+   test("check errors when a task lock references a missing spec", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root);
diff --git a/contracts/evidence-payloads/SCWBS-042.patch b/contracts/evidence-payloads/SCWBS-042.patch
new file mode 100644
index 0000000..0aa059c
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-042.patch
@@ -0,0 +1,87 @@
+diff --git a/contracts/tasks/SCWBS-042.yaml b/contracts/tasks/SCWBS-042.yaml
+new file mode 100644
+index 0000000..7befc5f
+--- /dev/null
++++ b/contracts/tasks/SCWBS-042.yaml
+@@ -0,0 +1,30 @@
++id: SCWBS-042
++type: task-contract
++wbsNodeId: node-governance-maintenance
++featureId: F-SCWBS-GOVERNANCE
++branchName: codex/scwbs-042-ai-next-task-evidence-filter
++allowedPaths:
++  - src/commands/ai-queue.ts
++  - tests/scwbs.test.ts
++  - contracts/tasks/SCWBS-042.yaml
++  - contracts/evidence/SCWBS-042.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths:
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - ai next-task excludes Task Contracts that already have Evidence.
++  - ai next-task still reports follow-up work when review or evidence follow-up remains.
++  - Existing planned-task dependency and Human Gate filters are preserved.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T00:00:00.000Z"
+diff --git a/src/commands/ai-queue.ts b/src/commands/ai-queue.ts
+index 72f493e..e968547 100644
+--- a/src/commands/ai-queue.ts
++++ b/src/commands/ai-queue.ts
+@@ -66,6 +66,7 @@ export function buildNextTask(root: string): string {
+   const candidates = tasks
+     .flatMap(({ task }) => {
+       if (!task || task.humanGateRequiredPaths.length > 0) return [];
++      if (evidenceExists(root, task.id)) return [];
+       const node = findNode(wbs, task.wbsNodeId);
+       if (!node) return [];
+       const status = node.status ?? "planned";
+diff --git a/tests/scwbs.test.ts b/tests/scwbs.test.ts
+index 7bc0894..9ea69eb 100644
+--- a/tests/scwbs.test.ts
++++ b/tests/scwbs.test.ts
+@@ -564,6 +564,34 @@ describe("scwbs MVP", () => {
+     expect(buildNextTask(root)).toBe("Planned task candidates:\n- WBS-001-004 | API Implementation | 1.1\n");
+   });
+ 
++  test("ai next-task excludes a planned task that already has evidence", () => {
++    const root = makeTempRepo();
++    const wbs = sampleWbs("planned");
++    wbs.nodes[0].status = "completed";
++    wbs.relations = [
++      ...(wbs.relations ?? []),
++      {
++        id: "rel-api-depends-on-root",
++        type: "dependsOn",
++        source: "node-api",
++        target: "node-root"
++      }
++    ];
++    writeScwbsProject(root);
++    writeJson(root, "contracts/wbs/project.wbs.json", wbs);
++    writeYaml(
++      root,
++      "contracts/tasks/WBS-001-004.yaml",
++      sampleTask({
++        humanGateRequiredPaths: []
++      }) as unknown as Record<string, unknown>
++    );
++    writeYaml(root, "contracts/evidence/WBS-001-004.yaml", sampleEvidence() as unknown as Record<string, unknown>);
++
++    expect(buildNextTask(root)).toContain("No available planned tasks.");
++    expect(buildNextTask(root)).toContain("Run `scwbs next` for Evidence or review guidance.");
++  });
++
+   test("ai next-task points to scwbs next when no planned task is available but evidence is missing", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root, "ready");
diff --git a/contracts/evidence-payloads/SCWBS-043.patch b/contracts/evidence-payloads/SCWBS-043.patch
new file mode 100644
index 0000000..1bff8a9
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-043.patch
@@ -0,0 +1,109 @@
+diff --git a/contracts/tasks/SCWBS-043.yaml b/contracts/tasks/SCWBS-043.yaml
+new file mode 100644
+index 0000000..f1dfe45
+--- /dev/null
++++ b/contracts/tasks/SCWBS-043.yaml
+@@ -0,0 +1,30 @@
++id: SCWBS-043
++type: task-contract
++wbsNodeId: node-governance-maintenance
++featureId: F-SCWBS-GOVERNANCE
++branchName: codex/scwbs-043-review-queue-shared-node-completion
++allowedPaths:
++  - src/commands/review-queue.ts
++  - tests/scwbs.test.ts
++  - contracts/tasks/SCWBS-043.yaml
++  - contracts/evidence/SCWBS-043.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths:
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - review-queue does not treat shared WBS nodes as directly ready for completion.
++  - review-queue explains that shared node completion requires a dedicated node-level completion task.
++  - Existing single-task review queue behavior is preserved.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T00:00:00.000Z"
+diff --git a/src/commands/review-queue.ts b/src/commands/review-queue.ts
+index 1da4875..9db7fcf 100644
+--- a/src/commands/review-queue.ts
++++ b/src/commands/review-queue.ts
+@@ -30,8 +30,14 @@ function incompleteDependencies(rootNodeId: string, wbs: ReturnType<typeof readW
+ export function buildReviewQueue(root: string): string {
+   const wbs = readWbs(root);
+   const entries: ReviewQueueEntry[] = [];
++  const tasks = listTasks(root);
++  const taskCountByNode = new Map<string, number>();
++  for (const entry of tasks) {
++    if (!entry.task) continue;
++    taskCountByNode.set(entry.task.wbsNodeId, (taskCountByNode.get(entry.task.wbsNodeId) ?? 0) + 1);
++  }
+ 
+-  for (const entry of listTasks(root)) {
++  for (const entry of tasks) {
+     const task = entry.task;
+     if (!task) continue;
+     const node = findNode(wbs, task.wbsNodeId);
+@@ -63,6 +69,9 @@ export function buildReviewQueue(root: string): string {
+         warnings.push(`dependsOn node ${blockedBy} is not completed`);
+       }
+     }
++    if ((taskCountByNode.get(node.id) ?? 0) > 1) {
++      completionBlockedBy.push("node has multiple Task Contracts; completion requires a dedicated node-level completion task");
++    }
+ 
+     if (evidence) {
+       const touchesHumanGate = evidence.changedFiles.some((file) => matchesAny(file, task.humanGateRequiredPaths));
+@@ -88,8 +97,11 @@ export function buildReviewQueue(root: string): string {
+ 
+     if (reasons.length > 0) {
+       const hasPullRequest = Boolean(evidence?.git?.pullRequest ?? approval?.pullRequest);
++      const sharedNodeCompletionBlocked = completionBlockedBy.some((item) => item.includes("multiple Task Contracts"));
+       const suggestedAction = completionBlockedBy.length > 0
+-        ? "review evidence now, but defer completion until dependencies are completed"
++        ? sharedNodeCompletionBlocked
++          ? "review evidence now, but defer WBS completion to a dedicated node-level completion task"
++          : "review evidence now, but defer completion until dependencies are completed"
+         : !hasPullRequest
+           ? "create or record PR, then human review for completion"
+           : !hasReview
+diff --git a/tests/scwbs.test.ts b/tests/scwbs.test.ts
+index 9ea69eb..d5f4db9 100644
+--- a/tests/scwbs.test.ts
++++ b/tests/scwbs.test.ts
+@@ -1432,6 +1432,25 @@ fs.writeFileSync(wbsPath, JSON.stringify(wbs, null, 2) + "\\n");
+     expect(queue).toContain("suggestedAction: review evidence now, but defer completion until dependencies are completed");
+   });
+ 
++  test("review queue defers completion for shared WBS nodes", () => {
++    const root = makeTempRepo();
++    writeScwbsProject(root, "planned");
++    writeYaml(root, "contracts/tasks/WBS-001-005.yaml", sampleTask({
++      id: "WBS-001-005",
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-004.yaml", sampleEvidence() as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-005.yaml", sampleEvidence({
++      id: "EVD-001-005",
++      taskId: "WBS-001-005"
++    }) as unknown as Record<string, unknown>);
++
++    const queue = buildReviewQueue(root);
++    expect(queue).toContain("completionBlockedBy: node has multiple Task Contracts; completion requires a dedicated node-level completion task");
++    expect(queue).toContain("suggestedAction: review evidence now, but defer WBS completion to a dedicated node-level completion task");
++    expect(queue).toContain("Ready for completion review:\n- None");
++  });
++
+   test("review queue shows pull request metadata when present", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root, "planned");
diff --git a/contracts/evidence-payloads/SCWBS-044.patch b/contracts/evidence-payloads/SCWBS-044.patch
new file mode 100644
index 0000000..09bcfd4
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-044.patch
@@ -0,0 +1,77 @@
+diff --git a/contracts/tasks/SCWBS-044.yaml b/contracts/tasks/SCWBS-044.yaml
+new file mode 100644
+index 0000000..9ed6508
+--- /dev/null
++++ b/contracts/tasks/SCWBS-044.yaml
+@@ -0,0 +1,29 @@
++id: SCWBS-044
++type: task-contract
++wbsNodeId: node-governance-maintenance
++featureId: F-SCWBS-GOVERNANCE
++branchName: codex/scwbs-044-review-health-blocker-label
++allowedPaths:
++  - src/commands/review-queue.ts
++  - tests/scwbs.test.ts
++  - contracts/tasks/SCWBS-044.yaml
++  - contracts/evidence/SCWBS-044.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths:
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - Review Health blocker count describes all completion blockers, not only incomplete dependencies.
++  - Existing per-entry dependency warnings remain unchanged.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T00:00:00.000Z"
+diff --git a/src/commands/review-queue.ts b/src/commands/review-queue.ts
+index 9db7fcf..d525604 100644
+--- a/src/commands/review-queue.ts
++++ b/src/commands/review-queue.ts
+@@ -138,7 +138,7 @@ export function buildReviewQueue(root: string): string {
+   lines.push("Review Health:");
+   lines.push(`- ${sortedEntries.length} review candidates`);
+   lines.push(`- ${missingPullRequestCount} candidates missing pull request metadata`);
+-  lines.push(`- ${blockedCount} candidates blocked by incomplete dependencies`);
++  lines.push(`- ${blockedCount} candidates blocked by completion prerequisites`);
+   lines.push(`- ${readyCount} candidates ready for completion review`);
+   lines.push("");
+ 
+diff --git a/tests/scwbs.test.ts b/tests/scwbs.test.ts
+index d5f4db9..913c021 100644
+--- a/tests/scwbs.test.ts
++++ b/tests/scwbs.test.ts
+@@ -1427,6 +1427,7 @@ fs.writeFileSync(wbsPath, JSON.stringify(wbs, null, 2) + "\\n");
+     writeYaml(root, "contracts/evidence/WBS-001-004.yaml", sampleEvidence() as unknown as Record<string, unknown>);
+     const queue = buildReviewQueue(root);
+     expect(queue).toContain("evidence exists and the WBS node is not completed");
++    expect(queue).toContain("- 1 candidates blocked by completion prerequisites");
+     expect(queue).toContain("warning: dependsOn node 1 Root is not completed");
+     expect(queue).toContain("completionBlockedBy: 1 Root");
+     expect(queue).toContain("suggestedAction: review evidence now, but defer completion until dependencies are completed");
+@@ -1446,6 +1447,7 @@ fs.writeFileSync(wbsPath, JSON.stringify(wbs, null, 2) + "\\n");
+     }) as unknown as Record<string, unknown>);
+ 
+     const queue = buildReviewQueue(root);
++    expect(queue).toContain("- 2 candidates blocked by completion prerequisites");
+     expect(queue).toContain("completionBlockedBy: node has multiple Task Contracts; completion requires a dedicated node-level completion task");
+     expect(queue).toContain("suggestedAction: review evidence now, but defer WBS completion to a dedicated node-level completion task");
+     expect(queue).toContain("Ready for completion review:\n- None");
+@@ -1602,7 +1604,7 @@ fs.writeFileSync(wbsPath, JSON.stringify(wbs, null, 2) + "\\n");
+     expect(queue).toContain("Review Health:");
+     expect(queue).toContain("- 1 review candidates");
+     expect(queue).toContain("- 1 candidates missing pull request metadata");
+-    expect(queue).toContain("- 1 candidates blocked by incomplete dependencies");
++    expect(queue).toContain("- 1 candidates blocked by completion prerequisites");
+     expect(queue).toContain("- 0 candidates ready for completion review");
+     expect(queue).toContain("Ready for completion review:");
+     expect(queue).toContain("Blocked review candidates:");
diff --git a/contracts/evidence-payloads/SCWBS-045.patch b/contracts/evidence-payloads/SCWBS-045.patch
new file mode 100644
index 0000000..a060249
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-045.patch
@@ -0,0 +1,126 @@
+diff --git a/contracts/tasks/SCWBS-045.yaml b/contracts/tasks/SCWBS-045.yaml
+new file mode 100644
+index 0000000..d77da23
+--- /dev/null
++++ b/contracts/tasks/SCWBS-045.yaml
+@@ -0,0 +1,30 @@
++id: SCWBS-045
++type: task-contract
++wbsNodeId: node-governance-maintenance
++featureId: F-SCWBS-GOVERNANCE
++branchName: codex/scwbs-045-next-respects-review-blockers
++allowedPaths:
++  - src/commands/next.ts
++  - tests/scwbs.test.ts
++  - contracts/tasks/SCWBS-045.yaml
++  - contracts/evidence/SCWBS-045.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths:
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - next uses the Ready for completion review section before suggesting human completion review.
++  - next does not suggest human completion review when every review candidate is blocked.
++  - Existing missing Evidence and unblocked review guidance remains covered.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T00:00:00.000Z"
+diff --git a/src/commands/next.ts b/src/commands/next.ts
+index d12bd26..f7d8242 100644
+--- a/src/commands/next.ts
++++ b/src/commands/next.ts
+@@ -7,6 +7,11 @@ function taskIdFromMessage(message: string): string | undefined {
+   return /\b[A-Z]+-\d+(?:-\d+)?\b/.exec(message)?.[0];
+ }
+ 
++function taskIdFromSection(queue: string, heading: string): string | undefined {
++  const section = new RegExp(`${heading}:\\n([\\s\\S]*?)(?:\\n\\n|$)`).exec(queue)?.[1] ?? "";
++  return /^- ([A-Z]+-\d+(?:-\d+)?)/m.exec(section)?.[1];
++}
++
+ export function buildNextAction(root: string): string {
+   const stale = collectCheckIssues(root).find((issue) => issue.code.startsWith("task.contractLock"));
+   if (stale) {
+@@ -36,7 +41,7 @@ Command:
+   }
+ 
+   const queue = buildReviewQueue(root);
+-  const reviewTask = /^- ([A-Z]+-\d+(?:-\d+)?)/m.exec(queue)?.[1];
++  const reviewTask = taskIdFromSection(queue, "Ready for completion review");
+   if (reviewTask) {
+     if (reviewExists(root, reviewTask)) {
+       return `Next suggested action:
+@@ -59,6 +64,18 @@ Command:
+   scwbs review request --task ${reviewTask}
+ `;
+   }
++  const blockedReviewTask = taskIdFromSection(queue, "Blocked review candidates");
++  if (blockedReviewTask) {
++    return `Next suggested action:
++
++Review blocked candidates
++Reason:
++- Review candidates exist, but completion is blocked by prerequisites
++
++Command:
++  scwbs review-queue
++`;
++  }
+ 
+   const nextTask = buildNextTask(root);
+   return `Next suggested action:
+diff --git a/tests/scwbs.test.ts b/tests/scwbs.test.ts
+index 913c021..6732a82 100644
+--- a/tests/scwbs.test.ts
++++ b/tests/scwbs.test.ts
+@@ -639,6 +639,41 @@ describe("scwbs MVP", () => {
+     expect(next).not.toContain("scwbs review request --task WBS-001-004");
+   });
+ 
++  test("next does not suggest completion review when review candidates are blocked", () => {
++    const root = makeTempRepo();
++    writeScwbsProject(root, "planned");
++    writeYaml(root, "contracts/tasks/WBS-001-005.yaml", sampleTask({
++      id: "WBS-001-005",
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-004.yaml", sampleEvidence({
++      git: {
++        branch: "task/WBS-001-004-api-implementation",
++        base: "main",
++        headCommit: "abc1234",
++        pullRequest: "#42"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-005.yaml", sampleEvidence({
++      id: "EVD-001-005",
++      taskId: "WBS-001-005"
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/reviews/WBS-001-004.yaml", {
++      id: "RVW-WBS-001-004",
++      type: "review",
++      taskId: "WBS-001-004",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#42",
++      groundTruth: ["contracts/tasks/WBS-001-004.yaml", "contracts/evidence/WBS-001-004.yaml"]
++    });
++
++    const next = buildNextAction(root);
++    expect(next).toContain("Review blocked candidates");
++    expect(next).toContain("completion is blocked by prerequisites");
++    expect(next).not.toContain("Human review for WBS-001-004");
++  });
++
+   test("health warns when evidence has only low-trust checks", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root, "completed");
diff --git a/contracts/evidence-payloads/SCWBS-046.patch b/contracts/evidence-payloads/SCWBS-046.patch
new file mode 100644
index 0000000..ce200e3
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-046.patch
@@ -0,0 +1,678 @@
+diff --git a/contracts/tasks/SCWBS-046.yaml b/contracts/tasks/SCWBS-046.yaml
+new file mode 100644
+index 0000000..37ecb9b
+--- /dev/null
++++ b/contracts/tasks/SCWBS-046.yaml
+@@ -0,0 +1,33 @@
++id: SCWBS-046
++type: task-contract
++wbsNodeId: node-governance-maintenance
++featureId: F-1-10
++branchName: codex/scwbs-046-node-level-completion-task
++allowedPaths:
++  - src/commands/completion.ts
++  - src/commands/review-queue.ts
++  - src/core/types.ts
++  - tests/scwbs.test.ts
++  - contracts/tasks/SCWBS-046.yaml
++  - contracts/evidence/SCWBS-046.yaml
++  - contracts/approvals/SCWBS-046.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths: []
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - Dedicated node-level completion tasks can aggregate evidence, review, approval, and PR metadata from multiple task contracts on the same WBS node.
++  - Shared-node completion is blocked unless every referenced task satisfies the same prerequisites that a direct task completion would require.
++  - review-queue and completion apply preserve root-node and unrelated-node safety while keeping single-task completion behavior intact.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T14:46:06.088Z"
+diff --git a/src/commands/completion.ts b/src/commands/completion.ts
+index abd75a7..9b3f4d2 100644
+--- a/src/commands/completion.ts
++++ b/src/commands/completion.ts
+@@ -1,10 +1,11 @@
+ import { mkdirSync, writeFileSync } from "node:fs";
+ import path from "node:path";
+-import { readApproval, readEvidence, readTask } from "../core/contracts.js";
++import { readApproval, readEvidence, readReview, readTask } from "../core/contracts.js";
++import { matchesAny } from "../core/glob.js";
+ import { approvalPath, resolveFrom } from "../core/paths.js";
+ import { stringifySimpleYaml } from "../core/yaml.js";
+ import { findNode, isDoneNode, readWbs } from "../core/wbs.js";
+-import type { ApprovalRecord, WbsDocument, WbsNode } from "../core/types.js";
++import type { ApprovalRecord, TaskContract, WbsDocument, WbsNode } from "../core/types.js";
+ import { buildApprovalApprove } from "./approval-request.js";
+ import { runRegistryRebuild } from "./registry-rebuild.js";
+ import { runWbsApply } from "./wbs.js";
+@@ -15,6 +16,14 @@ type CompletionPlanItem = {
+   pullRequest?: string;
+   approval: ApprovalRecord;
+   writesApproval: boolean;
++  completionTargets?: Array<{
++    taskId: string;
++    nodeCode: string;
++    nodeName: string;
++    pullRequest?: string;
++    approvalStatus?: string;
++    reviewStatus?: string;
++  }>;
+ };
+ 
+ type CompletionChangeSet = {
+@@ -50,11 +59,85 @@ function parseTaskIds(value: string | undefined): string[] {
+     .filter(Boolean);
+ }
+ 
++function completionTaskIds(task: TaskContract): string[] {
++  return [...new Set(task.completionTaskIds ?? [])];
++}
++
++function isNodeCompletionTask(task: TaskContract): boolean {
++  return task.completionScope === "node" && completionTaskIds(task).length > 0;
++}
++
+ function approvalFor(taskId: string, pullRequest: string | undefined, reason: string, existing: ApprovalRecord | undefined): CompletionPlanItem["approval"] {
+   if (existing?.status === "approved") return existing;
+   return buildApprovalApprove(taskId, { pullRequest, reason, approvedBy: "human" });
+ }
+ 
++function validateNodeCompletionTargets(root: string, wbs: WbsDocument, task: TaskContract): { blockers: string[]; targets: NonNullable<CompletionPlanItem["completionTargets"]> } {
++  const blockers: string[] = [];
++  const targets: NonNullable<CompletionPlanItem["completionTargets"]> = [];
++  const seen = new Set<string>();
++
++  for (const targetId of completionTaskIds(task)) {
++    if (targetId === task.id) {
++      blockers.push(`${task.id} completionTaskIds must not include itself`);
++      continue;
++    }
++    if (seen.has(targetId)) continue;
++    seen.add(targetId);
++
++    const { task: targetTask, issues: targetTaskIssues } = readTask(root, targetId);
++    if (!targetTask) {
++      blockers.push(targetTaskIssues.map((issue) => issue.message).join("\n"));
++      continue;
++    }
++
++    const targetNode = findNode(wbs, targetTask.wbsNodeId);
++    if (!targetNode) {
++      blockers.push(`${targetTask.id} references missing WBS node: ${targetTask.wbsNodeId}`);
++      continue;
++    }
++    if (targetNode.id !== task.wbsNodeId) {
++      blockers.push(`${targetTask.id} targets WBS node ${targetNode.id}, not ${task.wbsNodeId}`);
++      continue;
++    }
++
++    const { evidence, issues: evidenceIssues } = readEvidence(root, targetTask.id);
++    const { approval, issues: approvalIssues } = readApproval(root, targetTask.id);
++    const { review, issues: reviewIssues } = readReview(root, targetTask.id);
++    const missingEvidenceOnly = evidenceIssues.length === 1 && evidenceIssues[0]?.code === "evidence.missing";
++    const missingApprovalOnly = approvalIssues.length === 1 && approvalIssues[0]?.code === "approval.missing";
++    const missingReviewOnly = reviewIssues.length === 1 && reviewIssues[0]?.code === "review.missing";
++    const hasEvidence = Boolean(evidence) && !missingEvidenceOnly;
++    const hasApproval = Boolean(approval) && !missingApprovalOnly;
++    const hasReview = Boolean(review) && !missingReviewOnly;
++    const pullRequest = evidence?.git?.pullRequest ?? approval?.pullRequest;
++
++    if (!hasEvidence) blockers.push(`${targetTask.id} is missing evidence`);
++    if (!pullRequest) blockers.push(`${targetTask.id} is missing pull request metadata`);
++    if (!hasReview) blockers.push(`${targetTask.id} is missing review metadata`);
++    const touchesHumanGate = evidence?.changedFiles.some((file) => matchesAny(file, targetTask.humanGateRequiredPaths)) ?? false;
++    if (touchesHumanGate && !hasApproval) {
++      blockers.push(`${targetTask.id} changed human gate paths but no approved approval record exists`);
++    }
++    if (approval?.status === "requested") {
++      blockers.push(`${targetTask.id} approval is still requested`);
++    } else if (approval?.status === "rejected") {
++      blockers.push(`${targetTask.id} approval was rejected`);
++    }
++
++    targets.push({
++      taskId: targetTask.id,
++      nodeCode: targetNode.code,
++      nodeName: targetNode.name,
++      pullRequest,
++      approvalStatus: approval?.status,
++      reviewStatus: review?.status
++    });
++  }
++
++  return { blockers, targets };
++}
++
+ function buildCompletionChangeSet(wbs: WbsDocument, completionTaskId: string, reason: string, items: CompletionPlanItem[]): CompletionChangeSet {
+   return {
+     schemaVersion: "0.1.0",
+@@ -113,12 +196,28 @@ export function buildCompletionPlan(root: string, taskIdsValue: string | undefin
+ 
+     const pullRequest = evidence.git?.pullRequest ?? approval?.pullRequest;
+     if (!pullRequest) throw new Error(`${task.id} has no pull request metadata in Evidence or Approval`);
++
++    const { review, issues: reviewIssues } = readReview(root, task.id);
++    const missingReviewOnly = reviewIssues.length === 1 && reviewIssues[0]?.code === "review.missing";
++    if (isNodeCompletionTask(task) && !review && !missingReviewOnly) {
++      throw new Error(reviewIssues.map((issue) => issue.message).join("\n"));
++    }
++    if (isNodeCompletionTask(task) && !review) {
++      throw new Error(`${task.id} is a node-level completion task and requires review metadata before completion`);
++    }
++
++    const completionTargets = isNodeCompletionTask(task) ? validateNodeCompletionTargets(root, wbs, task) : { blockers: [], targets: [] };
++    if (isNodeCompletionTask(task) && completionTargets.blockers.length > 0) {
++      throw new Error(`${task.id} cannot complete shared WBS node ${node.id} until:\n- ${completionTargets.blockers.join("\n- ")}`);
++    }
++
+     items.push({
+       taskId: task.id,
+       node,
+       pullRequest,
+       approval: approvalFor(task.id, pullRequest, reason, approval),
+-      writesApproval: approval?.status !== "approved"
++      writesApproval: approval?.status !== "approved",
++      completionTargets: completionTargets.targets
+     });
+   }
+ 
+@@ -133,6 +232,17 @@ export function buildCompletionPreview(root: string, taskIdsValue: string | unde
+     lines.push(`- ${item.taskId}: ${item.node.code} ${item.node.name} -> completed`);
+     lines.push(`  pullRequest: ${item.pullRequest}`);
+     lines.push(`  approval: ${item.writesApproval ? "will write approved record" : "already approved"}`);
++    if (item.completionTargets && item.completionTargets.length > 0) {
++      lines.push("  completionTargets:");
++      for (const target of item.completionTargets) {
++        const details = [
++          target.pullRequest ? `PR ${target.pullRequest}` : "no PR",
++          target.reviewStatus ? `review ${target.reviewStatus}` : "no review",
++          target.approvalStatus ? `approval ${target.approvalStatus}` : "no approval"
++        ].join(", ");
++        lines.push(`    - ${target.taskId}: ${target.nodeCode} ${target.nodeName} (${details})`);
++      }
++    }
+   }
+   lines.push("");
+   lines.push(`changeset: contracts/changesets/${completionTaskId}-complete-reviewed-work.json`);
+diff --git a/src/commands/review-queue.ts b/src/commands/review-queue.ts
+index d525604..4837153 100644
+--- a/src/commands/review-queue.ts
++++ b/src/commands/review-queue.ts
+@@ -1,6 +1,7 @@
+ import { readApproval, listTasks, readEvidence, readReview } from "../core/contracts.js";
+ import { matchesAny } from "../core/glob.js";
+ import { findNode, isDoneNode, readWbs } from "../core/wbs.js";
++import type { TaskContract } from "../core/types.js";
+ 
+ type ReviewQueueEntry = {
+   taskId: string;
+@@ -9,6 +10,7 @@ type ReviewQueueEntry = {
+   reasons: string[];
+   warnings: string[];
+   completionBlockedBy: string[];
++  completionTargets?: NodeCompletionTarget[];
+   branchName?: string;
+   pullRequest?: string;
+   approvalStatus?: string;
+@@ -16,6 +18,90 @@ type ReviewQueueEntry = {
+   suggestedAction: string;
+ };
+ 
++type NodeCompletionTarget = {
++  taskId: string;
++  nodeCode: string;
++  nodeName: string;
++  pullRequest?: string;
++  approvalStatus?: string;
++  reviewStatus?: string;
++};
++
++function completionTaskIds(task: TaskContract): string[] {
++  return [...new Set(task.completionTaskIds ?? [])];
++}
++
++function isNodeCompletionTask(task: TaskContract): boolean {
++  return task.completionScope === "node" && completionTaskIds(task).length > 0;
++}
++
++function collectNodeCompletionTargets(root: string, wbs: ReturnType<typeof readWbs>, task: TaskContract): { blockers: string[]; targets: NodeCompletionTarget[] } {
++  const blockers: string[] = [];
++  const targets: NodeCompletionTarget[] = [];
++  const seen = new Set<string>();
++
++  for (const targetId of completionTaskIds(task)) {
++    if (targetId === task.id) {
++      blockers.push(`${task.id} completionTaskIds must not include itself`);
++      continue;
++    }
++    if (seen.has(targetId)) continue;
++    seen.add(targetId);
++
++    const taskEntry = listTasks(root).find((entry) => entry.task?.id === targetId);
++    const targetTask = taskEntry?.task;
++    if (!targetTask) {
++      blockers.push(`missing completion target task ${targetId}`);
++      continue;
++    }
++
++    const targetNode = findNode(wbs, targetTask.wbsNodeId);
++    if (!targetNode) {
++      blockers.push(`${targetTask.id} references missing WBS node: ${targetTask.wbsNodeId}`);
++      continue;
++    }
++    if (targetNode.id !== task.wbsNodeId) {
++      blockers.push(`${targetTask.id} targets WBS node ${targetNode.id}, not ${task.wbsNodeId}`);
++      continue;
++    }
++
++    const { evidence, issues } = readEvidence(root, targetTask.id);
++    const { approval, issues: approvalIssues } = readApproval(root, targetTask.id);
++    const { review, issues: reviewIssues } = readReview(root, targetTask.id);
++    const missingEvidenceOnly = issues.length === 1 && issues[0]?.code === "evidence.missing";
++    const missingApprovalOnly = approvalIssues.length === 1 && approvalIssues[0]?.code === "approval.missing";
++    const missingReviewOnly = reviewIssues.length === 1 && reviewIssues[0]?.code === "review.missing";
++    const hasEvidence = Boolean(evidence) && !missingEvidenceOnly;
++    const hasApproval = Boolean(approval) && !missingApprovalOnly;
++    const hasReview = Boolean(review) && !missingReviewOnly;
++    const pullRequest = evidence?.git?.pullRequest ?? approval?.pullRequest;
++
++    if (!hasEvidence) blockers.push(`${targetTask.id} is missing evidence`);
++    if (!pullRequest) blockers.push(`${targetTask.id} is missing pull request metadata`);
++    if (!hasReview) blockers.push(`${targetTask.id} is missing review metadata`);
++    const touchesHumanGate = evidence?.changedFiles.some((file) => matchesAny(file, targetTask.humanGateRequiredPaths)) ?? false;
++    if (touchesHumanGate && !hasApproval) {
++      blockers.push(`${targetTask.id} changed human gate paths but no approved approval record exists`);
++    }
++    if (approval?.status === "requested") {
++      blockers.push(`${targetTask.id} approval is still requested`);
++    } else if (approval?.status === "rejected") {
++      blockers.push(`${targetTask.id} approval was rejected`);
++    }
++
++    targets.push({
++      taskId: targetTask.id,
++      nodeCode: targetNode.code,
++      nodeName: targetNode.name,
++      pullRequest,
++      approvalStatus: approval?.status,
++      reviewStatus: review?.status
++    });
++  }
++
++  return { blockers, targets };
++}
++
+ function incompleteDependencies(rootNodeId: string, wbs: ReturnType<typeof readWbs>): string[] {
+   const nodesById = new Map(wbs.nodes.map((node) => [node.id, node]));
+   return (wbs.relations ?? [])
+@@ -46,6 +132,7 @@ export function buildReviewQueue(root: string): string {
+     const reasons: string[] = [];
+     const warnings: string[] = [];
+     const completionBlockedBy = incompleteDependencies(node.id, wbs);
++    const nodeCompletionTargets = isNodeCompletionTask(task) ? collectNodeCompletionTargets(root, wbs, task) : { blockers: [], targets: [] as NodeCompletionTarget[] };
+     const { evidence, issues } = readEvidence(root, task.id);
+     const { approval, issues: approvalIssues } = readApproval(root, task.id);
+     const { review, issues: reviewIssues } = readReview(root, task.id);
+@@ -69,9 +156,14 @@ export function buildReviewQueue(root: string): string {
+         warnings.push(`dependsOn node ${blockedBy} is not completed`);
+       }
+     }
+-    if ((taskCountByNode.get(node.id) ?? 0) > 1) {
++    if (!isNodeCompletionTask(task) && (taskCountByNode.get(node.id) ?? 0) > 1) {
+       completionBlockedBy.push("node has multiple Task Contracts; completion requires a dedicated node-level completion task");
+     }
++    if (isNodeCompletionTask(task) && nodeCompletionTargets.blockers.length > 0) {
++      for (const blockedBy of nodeCompletionTargets.blockers) {
++        completionBlockedBy.push(blockedBy);
++      }
++    }
+ 
+     if (evidence) {
+       const touchesHumanGate = evidence.changedFiles.some((file) => matchesAny(file, task.humanGateRequiredPaths));
+@@ -98,8 +190,11 @@ export function buildReviewQueue(root: string): string {
+     if (reasons.length > 0) {
+       const hasPullRequest = Boolean(evidence?.git?.pullRequest ?? approval?.pullRequest);
+       const sharedNodeCompletionBlocked = completionBlockedBy.some((item) => item.includes("multiple Task Contracts"));
++      const nodeCompletionTaskBlocked = isNodeCompletionTask(task) && completionBlockedBy.length > 0;
+       const suggestedAction = completionBlockedBy.length > 0
+-        ? sharedNodeCompletionBlocked
++        ? nodeCompletionTaskBlocked
++          ? "review evidence now, but defer node completion until the completion targets are ready"
++          : sharedNodeCompletionBlocked
+           ? "review evidence now, but defer WBS completion to a dedicated node-level completion task"
+           : "review evidence now, but defer completion until dependencies are completed"
+         : !hasPullRequest
+@@ -114,6 +209,7 @@ export function buildReviewQueue(root: string): string {
+         reasons,
+         warnings,
+         completionBlockedBy,
++        completionTargets: nodeCompletionTargets.targets,
+         branchName: evidence?.git?.branch ?? task.branchName,
+         pullRequest: evidence?.git?.pullRequest ?? approval?.pullRequest,
+         approvalStatus: approval?.status,
+@@ -165,6 +261,17 @@ export function buildReviewQueue(root: string): string {
+     for (const blockedBy of item.completionBlockedBy) {
+       lines.push(`  completionBlockedBy: ${blockedBy}`);
+     }
++    if (item.completionTargets && item.completionTargets.length > 0) {
++      lines.push("  completionTargets:");
++      for (const target of item.completionTargets) {
++        const details = [
++          target.pullRequest ? `PR ${target.pullRequest}` : "no PR",
++          target.reviewStatus ? `review ${target.reviewStatus}` : "no review",
++          target.approvalStatus ? `approval ${target.approvalStatus}` : "no approval"
++        ].join(", ");
++        lines.push(`    - ${target.taskId}: ${target.nodeCode} ${target.nodeName} (${details})`);
++      }
++    }
+     lines.push(`  suggestedAction: ${item.suggestedAction}`);
+   }
+ 
+diff --git a/src/core/types.ts b/src/core/types.ts
+index 2fe4904..c1f6ee1 100644
+--- a/src/core/types.ts
++++ b/src/core/types.ts
+@@ -67,6 +67,8 @@ export type TaskContract = {
+   id: string;
+   type: "task-contract";
+   mode?: "lite";
++  completionScope?: "node";
++  completionTaskIds?: string[];
+   wbsNodeId: string;
+   featureId: string;
+   branchName?: string;
+diff --git a/tests/scwbs.test.ts b/tests/scwbs.test.ts
+index 6732a82..90fb194 100644
+--- a/tests/scwbs.test.ts
++++ b/tests/scwbs.test.ts
+@@ -1246,6 +1246,88 @@ describe("scwbs MVP", () => {
+     expect(readApproval(root, "WBS-001-004").approval).toBeUndefined();
+   });
+ 
++  test("completion apply dry-run previews node-level completion targets", () => {
++    const root = makeTempRepo();
++    writeScwbsProject(root, "ready");
++    writeYaml(root, "contracts/tasks/WBS-001-005.yaml", sampleTask({
++      id: "WBS-001-005",
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/tasks/WBS-001-006.yaml", sampleTask({
++      id: "WBS-001-006",
++      wbsNodeId: "node-api",
++      branchName: "codex/wbs-001-006-node-completion",
++      completionScope: "node",
++      completionTaskIds: ["WBS-001-004", "WBS-001-005"],
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-004.yaml", sampleEvidence({
++      id: "EVD-001-004",
++      taskId: "WBS-001-004",
++      git: {
++        branch: "codex/wbs-001-004-api",
++        base: "main",
++        headCommit: "abc1234",
++        pullRequest: "#41"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-005.yaml", sampleEvidence({
++      id: "EVD-001-005",
++      taskId: "WBS-001-005",
++      git: {
++        branch: "codex/wbs-001-005-api",
++        base: "main",
++        headCommit: "abc1235",
++        pullRequest: "#42"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-006.yaml", sampleEvidence({
++      id: "EVD-001-006",
++      taskId: "WBS-001-006",
++      git: {
++        branch: "codex/wbs-001-006-node-completion",
++        base: "main",
++        headCommit: "abc1236",
++        pullRequest: "#43"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/reviews/WBS-001-004.yaml", {
++      id: "RVW-WBS-001-004",
++      type: "review",
++      taskId: "WBS-001-004",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#41",
++      groundTruth: ["contracts/tasks/WBS-001-004.yaml", "contracts/evidence/WBS-001-004.yaml"]
++    });
++    writeYaml(root, "contracts/reviews/WBS-001-005.yaml", {
++      id: "RVW-WBS-001-005",
++      type: "review",
++      taskId: "WBS-001-005",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#42",
++      groundTruth: ["contracts/tasks/WBS-001-005.yaml", "contracts/evidence/WBS-001-005.yaml"]
++    });
++    writeYaml(root, "contracts/reviews/WBS-001-006.yaml", {
++      id: "RVW-WBS-001-006",
++      type: "review",
++      taskId: "WBS-001-006",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#43",
++      groundTruth: ["contracts/tasks/WBS-001-006.yaml", "contracts/evidence/WBS-001-006.yaml"]
++    });
++
++    const preview = buildCompletionPreview(root, "WBS-001-006", "WBS-001-999", { reason: "Reviewed and accepted", allowRoot: false });
++    expect(preview).toContain("Completion apply dry-run:");
++    expect(preview).toContain("- WBS-001-006: 1.1 API Implementation -> completed");
++    expect(preview).toContain("completionTargets:");
++    expect(preview).toContain("- WBS-001-004: 1.1 API Implementation");
++    expect(preview).toContain("- WBS-001-005: 1.1 API Implementation");
++    expect(preview).toContain("approval: will write approved record");
++  });
++
+   test("completion apply rejects root node completion by default", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root, "ready");
+@@ -1296,6 +1378,101 @@ fs.writeFileSync(wbsPath, JSON.stringify(wbs, null, 2) + "\\n");
+     expect(registry).toContain("type: approval");
+   });
+ 
++  test("completion apply writes approvals for node-level completion tasks and completes the shared node", () => {
++    const root = makeTempRepo();
++    mkdirSync(path.join(root, "wjs/tools"), { recursive: true });
++    writeText(root, "wjs/tools/apply.ts", `
++const fs = require("node:fs");
++const [wbsPath, changeSetPath] = process.argv.slice(2);
++const wbs = JSON.parse(fs.readFileSync(wbsPath, "utf8"));
++const changeSet = JSON.parse(fs.readFileSync(changeSetPath, "utf8"));
++for (const operation of changeSet.operations) {
++  const node = wbs.nodes.find((item) => item.id === operation.nodeId);
++  if (node) node.status = operation.status;
++}
++fs.writeFileSync(wbsPath, JSON.stringify(wbs, null, 2) + "\\n");
++`);
++    writeScwbsProject(root, "ready");
++    writeYaml(root, "contracts/tasks/WBS-001-005.yaml", sampleTask({
++      id: "WBS-001-005",
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/tasks/WBS-001-006.yaml", sampleTask({
++      id: "WBS-001-006",
++      wbsNodeId: "node-api",
++      branchName: "codex/wbs-001-006-node-completion",
++      completionScope: "node",
++      completionTaskIds: ["WBS-001-004", "WBS-001-005"],
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-004.yaml", sampleEvidence({
++      id: "EVD-001-004",
++      taskId: "WBS-001-004",
++      git: {
++        branch: "codex/wbs-001-004-api",
++        base: "main",
++        headCommit: "abc1234",
++        pullRequest: "#41"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-005.yaml", sampleEvidence({
++      id: "EVD-001-005",
++      taskId: "WBS-001-005",
++      git: {
++        branch: "codex/wbs-001-005-api",
++        base: "main",
++        headCommit: "abc1235",
++        pullRequest: "#42"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-006.yaml", sampleEvidence({
++      id: "EVD-001-006",
++      taskId: "WBS-001-006",
++      git: {
++        branch: "codex/wbs-001-006-node-completion",
++        base: "main",
++        headCommit: "abc1236",
++        pullRequest: "#43"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/reviews/WBS-001-004.yaml", {
++      id: "RVW-WBS-001-004",
++      type: "review",
++      taskId: "WBS-001-004",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#41",
++      groundTruth: ["contracts/tasks/WBS-001-004.yaml", "contracts/evidence/WBS-001-004.yaml"]
++    });
++    writeYaml(root, "contracts/reviews/WBS-001-005.yaml", {
++      id: "RVW-WBS-001-005",
++      type: "review",
++      taskId: "WBS-001-005",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#42",
++      groundTruth: ["contracts/tasks/WBS-001-005.yaml", "contracts/evidence/WBS-001-005.yaml"]
++    });
++    writeYaml(root, "contracts/reviews/WBS-001-006.yaml", {
++      id: "RVW-WBS-001-006",
++      type: "review",
++      taskId: "WBS-001-006",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#43",
++      groundTruth: ["contracts/tasks/WBS-001-006.yaml", "contracts/evidence/WBS-001-006.yaml"]
++    });
++
++    expect(runCompletionApply(root, "WBS-001-006", "WBS-001-999", { reason: "Reviewed and accepted", apply: true, allowRoot: false })).toBe(0);
++    expect(readApproval(root, "WBS-001-006").approval?.status).toBe("approved");
++    expect(readFileSync(path.join(root, "contracts/changesets/WBS-001-999-complete-reviewed-work.json"), "utf8")).toContain("\"nodeId\": \"node-api\"");
++    const wbs = JSON.parse(readFileSync(path.join(root, "contracts/wbs/project.wbs.json"), "utf8"));
++    expect(wbs.nodes.find((node: { id: string; status: string }) => node.id === "node-api")?.status).toBe("completed");
++    const registry = readFileSync(path.join(root, "contracts/registry.yaml"), "utf8");
++    expect(registry).toContain("id: APR-WBS-001-006");
++    expect(registry).toContain("type: approval");
++  });
++
+   test("doctor reports suggested fixes for stale contracts", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root);
+@@ -1488,6 +1665,89 @@ fs.writeFileSync(wbsPath, JSON.stringify(wbs, null, 2) + "\\n");
+     expect(queue).toContain("Ready for completion review:\n- None");
+   });
+ 
++  test("review queue allows a dedicated node completion task to aggregate shared-node prerequisites", () => {
++    const root = makeTempRepo();
++    writeScwbsProject(root, "ready");
++    writeYaml(root, "contracts/tasks/WBS-001-005.yaml", sampleTask({
++      id: "WBS-001-005",
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/tasks/WBS-001-006.yaml", sampleTask({
++      id: "WBS-001-006",
++      wbsNodeId: "node-api",
++      branchName: "codex/wbs-001-006-node-completion",
++      completionScope: "node",
++      completionTaskIds: ["WBS-001-004", "WBS-001-005"],
++      humanGateRequiredPaths: []
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-004.yaml", sampleEvidence({
++      id: "EVD-001-004",
++      taskId: "WBS-001-004",
++      git: {
++        branch: "codex/wbs-001-004-api",
++        base: "main",
++        headCommit: "abc1234",
++        pullRequest: "#41"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-005.yaml", sampleEvidence({
++      id: "EVD-001-005",
++      taskId: "WBS-001-005",
++      git: {
++        branch: "codex/wbs-001-005-api",
++        base: "main",
++        headCommit: "abc1235",
++        pullRequest: "#42"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/evidence/WBS-001-006.yaml", sampleEvidence({
++      id: "EVD-001-006",
++      taskId: "WBS-001-006",
++      git: {
++        branch: "codex/wbs-001-006-node-completion",
++        base: "main",
++        headCommit: "abc1236",
++        pullRequest: "#43"
++      }
++    }) as unknown as Record<string, unknown>);
++    writeYaml(root, "contracts/reviews/WBS-001-004.yaml", {
++      id: "RVW-WBS-001-004",
++      type: "review",
++      taskId: "WBS-001-004",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#41",
++      groundTruth: ["contracts/tasks/WBS-001-004.yaml", "contracts/evidence/WBS-001-004.yaml"]
++    });
++    writeYaml(root, "contracts/reviews/WBS-001-005.yaml", {
++      id: "RVW-WBS-001-005",
++      type: "review",
++      taskId: "WBS-001-005",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#42",
++      groundTruth: ["contracts/tasks/WBS-001-005.yaml", "contracts/evidence/WBS-001-005.yaml"]
++    });
++    writeYaml(root, "contracts/reviews/WBS-001-006.yaml", {
++      id: "RVW-WBS-001-006",
++      type: "review",
++      taskId: "WBS-001-006",
++      status: "requested",
++      reviewProfile: "independent-ai-review",
++      pullRequest: "#43",
++      groundTruth: ["contracts/tasks/WBS-001-006.yaml", "contracts/evidence/WBS-001-006.yaml"]
++    });
++
++    const queue = buildReviewQueue(root);
++    expect(queue).toContain("WBS-001-006 | 1.1 | API Implementation");
++    expect(queue).toContain("completionTargets:");
++    expect(queue).toContain("- WBS-001-006");
++    expect(queue).toContain("Ready for completion review:");
++    expect(queue).toContain("- WBS-001-006");
++    expect(queue).toContain("- WBS-001-004 blocked by node has multiple Task Contracts; completion requires a dedicated node-level completion task");
++    expect(queue).toContain("- WBS-001-005 blocked by node has multiple Task Contracts; completion requires a dedicated node-level completion task");
++  });
++
+   test("review queue shows pull request metadata when present", () => {
+     const root = makeTempRepo();
+     writeScwbsProject(root, "planned");
diff --git a/contracts/evidence-payloads/SCWBS-047.patch b/contracts/evidence-payloads/SCWBS-047.patch
new file mode 100644
index 0000000..867a649
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-047.patch
@@ -0,0 +1,52 @@
+diff --git a/contracts/tasks/SCWBS-046.yaml b/contracts/tasks/SCWBS-046.yaml
+index 37ecb9b..1089ba0 100644
+--- a/contracts/tasks/SCWBS-046.yaml
++++ b/contracts/tasks/SCWBS-046.yaml
+@@ -1,5 +1,12 @@
+ id: SCWBS-046
+ type: task-contract
++completionScope: node
++completionTaskIds:
++  - SCWBS-041
++  - SCWBS-042
++  - SCWBS-043
++  - SCWBS-044
++  - SCWBS-045
+ wbsNodeId: node-governance-maintenance
+ featureId: F-1-10
+ branchName: codex/scwbs-046-node-level-completion-task
+diff --git a/contracts/tasks/SCWBS-047.yaml b/contracts/tasks/SCWBS-047.yaml
+new file mode 100644
+index 0000000..b10525b
+--- /dev/null
++++ b/contracts/tasks/SCWBS-047.yaml
+@@ -0,0 +1,29 @@
++id: SCWBS-047
++type: task-contract
++wbsNodeId: node-governance-maintenance
++featureId: F-1-10
++branchName: codex/scwbs-047-node-completion-contract-metadata
++allowedPaths:
++  - contracts/tasks/SCWBS-046.yaml
++  - contracts/tasks/SCWBS-047.yaml
++  - contracts/evidence/SCWBS-047.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths:
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - SCWBS-046 is explicitly marked as a node-level completion task for node-governance-maintenance.
++  - SCWBS-046 completion targets enumerate the shared-node task contracts that must be ready before node completion.
++  - review-queue and completion apply recognize SCWBS-046 as the dedicated node-level completion task instead of a generic shared-node candidate.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T17:12:29.932Z"
diff --git a/contracts/evidence-payloads/SCWBS-048.patch b/contracts/evidence-payloads/SCWBS-048.patch
new file mode 100644
index 0000000..11953b7
--- /dev/null
+++ b/contracts/evidence-payloads/SCWBS-048.patch
@@ -0,0 +1,211 @@
+diff --git a/contracts/evidence/SCWBS-041.yaml b/contracts/evidence/SCWBS-041.yaml
+index c16a9d9..dadb966 100644
+--- a/contracts/evidence/SCWBS-041.yaml
++++ b/contracts/evidence/SCWBS-041.yaml
+@@ -7,6 +7,7 @@ git:
+   base: origin/main
+   baseCommit: d11e758f6e38be57abf51647188d560fe335c6ce
+   changedFilesBasis: branch-diff
++  pullRequest: "#43"
+   headCommit: 573796f072b40fce28017ac75d972b912c04941b
+ changedFiles:
+   - contracts/registry.yaml
+diff --git a/contracts/evidence/SCWBS-042.yaml b/contracts/evidence/SCWBS-042.yaml
+index 56d805b..bbdb445 100644
+--- a/contracts/evidence/SCWBS-042.yaml
++++ b/contracts/evidence/SCWBS-042.yaml
+@@ -7,6 +7,7 @@ git:
+   base: origin/main
+   baseCommit: 4270260bbb696d71bb7aec27639fbaff4eff76ad
+   changedFilesBasis: branch-diff
++  pullRequest: "#44"
+   headCommit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+ changedFiles:
+   - contracts/registry.yaml
+diff --git a/contracts/evidence/SCWBS-043.yaml b/contracts/evidence/SCWBS-043.yaml
+index 23f3971..938891e 100644
+--- a/contracts/evidence/SCWBS-043.yaml
++++ b/contracts/evidence/SCWBS-043.yaml
+@@ -7,6 +7,7 @@ git:
+   base: origin/main
+   baseCommit: 8d15b78ef9c6d13f4b6306783c0afbc94e486333
+   changedFilesBasis: branch-diff
++  pullRequest: "#45"
+   headCommit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+ changedFiles:
+   - contracts/registry.yaml
+diff --git a/contracts/evidence/SCWBS-044.yaml b/contracts/evidence/SCWBS-044.yaml
+index d9adb7c..5a6f1b0 100644
+--- a/contracts/evidence/SCWBS-044.yaml
++++ b/contracts/evidence/SCWBS-044.yaml
+@@ -7,6 +7,7 @@ git:
+   base: origin/main
+   baseCommit: f5eff9a1d1c1cfa7991d3e55f22b38bde7f5d9da
+   changedFilesBasis: branch-diff
++  pullRequest: "#46"
+   headCommit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+ changedFiles:
+   - contracts/registry.yaml
+diff --git a/contracts/evidence/SCWBS-045.yaml b/contracts/evidence/SCWBS-045.yaml
+index 27c6e27..148181b 100644
+--- a/contracts/evidence/SCWBS-045.yaml
++++ b/contracts/evidence/SCWBS-045.yaml
+@@ -7,6 +7,7 @@ git:
+   base: origin/main
+   baseCommit: 3b51f1aab0edc401a68bba0f3809082622d055b0
+   changedFilesBasis: branch-diff
++  pullRequest: "#47"
+   headCommit: d541daa873978f88e8d693f43e64f41a535f4c38
+ changedFiles:
+   - contracts/registry.yaml
+diff --git a/contracts/evidence/SCWBS-046.yaml b/contracts/evidence/SCWBS-046.yaml
+index 3f259a4..a189a4b 100644
+--- a/contracts/evidence/SCWBS-046.yaml
++++ b/contracts/evidence/SCWBS-046.yaml
+@@ -7,6 +7,7 @@ git:
+   base: origin/main
+   baseCommit: 80630d2b484a93b2c7871cbd071a7bf26ff0a052
+   changedFilesBasis: branch-diff
++  pullRequest: "#48"
+   headCommit: aad4134dd7d49c55cc86aface971884cc443c7cb
+ changedFiles:
+   - contracts/tasks/SCWBS-046.yaml
+diff --git a/contracts/reviews/SCWBS-041.yaml b/contracts/reviews/SCWBS-041.yaml
+new file mode 100644
+index 0000000..74e5fd1
+--- /dev/null
++++ b/contracts/reviews/SCWBS-041.yaml
+@@ -0,0 +1,9 @@
++id: RVW-SCWBS-041
++type: review
++taskId: SCWBS-041
++status: requested
++reviewProfile: independent-ai-review
++pullRequest: "#43"
++groundTruth:
++  - contracts/tasks/SCWBS-041.yaml
++  - contracts/evidence/SCWBS-041.yaml
+diff --git a/contracts/reviews/SCWBS-042.yaml b/contracts/reviews/SCWBS-042.yaml
+new file mode 100644
+index 0000000..83f4c65
+--- /dev/null
++++ b/contracts/reviews/SCWBS-042.yaml
+@@ -0,0 +1,9 @@
++id: RVW-SCWBS-042
++type: review
++taskId: SCWBS-042
++status: requested
++reviewProfile: independent-ai-review
++pullRequest: "#44"
++groundTruth:
++  - contracts/tasks/SCWBS-042.yaml
++  - contracts/evidence/SCWBS-042.yaml
+diff --git a/contracts/reviews/SCWBS-043.yaml b/contracts/reviews/SCWBS-043.yaml
+new file mode 100644
+index 0000000..a6f2f6b
+--- /dev/null
++++ b/contracts/reviews/SCWBS-043.yaml
+@@ -0,0 +1,9 @@
++id: RVW-SCWBS-043
++type: review
++taskId: SCWBS-043
++status: requested
++reviewProfile: independent-ai-review
++pullRequest: "#45"
++groundTruth:
++  - contracts/tasks/SCWBS-043.yaml
++  - contracts/evidence/SCWBS-043.yaml
+diff --git a/contracts/reviews/SCWBS-044.yaml b/contracts/reviews/SCWBS-044.yaml
+new file mode 100644
+index 0000000..6c44110
+--- /dev/null
++++ b/contracts/reviews/SCWBS-044.yaml
+@@ -0,0 +1,9 @@
++id: RVW-SCWBS-044
++type: review
++taskId: SCWBS-044
++status: requested
++reviewProfile: independent-ai-review
++pullRequest: "#46"
++groundTruth:
++  - contracts/tasks/SCWBS-044.yaml
++  - contracts/evidence/SCWBS-044.yaml
+diff --git a/contracts/reviews/SCWBS-045.yaml b/contracts/reviews/SCWBS-045.yaml
+new file mode 100644
+index 0000000..86f6222
+--- /dev/null
++++ b/contracts/reviews/SCWBS-045.yaml
+@@ -0,0 +1,9 @@
++id: RVW-SCWBS-045
++type: review
++taskId: SCWBS-045
++status: requested
++reviewProfile: independent-ai-review
++pullRequest: "#47"
++groundTruth:
++  - contracts/tasks/SCWBS-045.yaml
++  - contracts/evidence/SCWBS-045.yaml
+diff --git a/contracts/reviews/SCWBS-046.yaml b/contracts/reviews/SCWBS-046.yaml
+new file mode 100644
+index 0000000..b6d4f62
+--- /dev/null
++++ b/contracts/reviews/SCWBS-046.yaml
+@@ -0,0 +1,9 @@
++id: RVW-SCWBS-046
++type: review
++taskId: SCWBS-046
++status: requested
++reviewProfile: independent-ai-review
++pullRequest: "#48"
++groundTruth:
++  - contracts/tasks/SCWBS-046.yaml
++  - contracts/evidence/SCWBS-046.yaml
+diff --git a/contracts/tasks/SCWBS-048.yaml b/contracts/tasks/SCWBS-048.yaml
+new file mode 100644
+index 0000000..7cb25c0
+--- /dev/null
++++ b/contracts/tasks/SCWBS-048.yaml
+@@ -0,0 +1,43 @@
++id: SCWBS-048
++type: task-contract
++completionScope: node
++completionTaskIds:
++  - SCWBS-046
++wbsNodeId: node-governance-maintenance
++featureId: F-1-10
++branchName: codex/scwbs-048-review-metadata-backfill
++allowedPaths:
++  - contracts/evidence/SCWBS-041.yaml
++  - contracts/evidence/SCWBS-042.yaml
++  - contracts/evidence/SCWBS-043.yaml
++  - contracts/evidence/SCWBS-044.yaml
++  - contracts/evidence/SCWBS-045.yaml
++  - contracts/evidence/SCWBS-046.yaml
++  - contracts/reviews/SCWBS-041.yaml
++  - contracts/reviews/SCWBS-042.yaml
++  - contracts/reviews/SCWBS-043.yaml
++  - contracts/reviews/SCWBS-044.yaml
++  - contracts/reviews/SCWBS-045.yaml
++  - contracts/reviews/SCWBS-046.yaml
++  - contracts/tasks/SCWBS-048.yaml
++  - contracts/evidence/SCWBS-048.yaml
++  - contracts/registry.yaml
++forbiddenPaths:
++  - wjs/**
++humanGateRequiredPaths:
++requiredChecks:
++  - test
++  - typecheck
++  - build
++doneCriteria:
++  - SCWBS-041 through SCWBS-046 have pull request metadata recorded in Evidence and corresponding review records.
++  - SCWBS-046 no longer reports missing PR metadata or missing review metadata for its completion targets.
++  - review-queue reflects the backfilled metadata and keeps the node-level completion path distinct from generic shared-node completion.
++evidenceRequired:
++  - test-result
++  - typecheck-result
++  - build-result
++contractLock:
++  wbsRevision: "sha256:a14ccfee62771d89523fa01fdfce3be720b9a3feda3026ebbf24d15456d82457"
++  wbsNodeId: node-governance-maintenance
++  createdAt: "2026-07-02T17:22:51.646Z"
diff --git a/contracts/evidence/SCWBS-041.yaml b/contracts/evidence/SCWBS-041.yaml
index 772b2d7..d858fbb 100644
--- a/contracts/evidence/SCWBS-041.yaml
+++ b/contracts/evidence/SCWBS-041.yaml
@@ -2,15 +2,29 @@ id: EVD-SCWBS-041
 type: evidence
 taskId: SCWBS-041
 commit: 573796f072b40fce28017ac75d972b912c04941b
+subjectHeadCommit: 573796f072b40fce28017ac75d972b912c04941b
+diffHash: sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: 573796f072b40fce28017ac75d972b912c04941b
+    treeHash: 0b5fb5e0bcb381dcd9c13e4ec400d3f956dbc68e
+    diffHash: sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-041.patch
+    manifestHash: sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
 git:
   branch: codex/scwbs-041-spec-change-proposal
   base: origin/main
   baseCommit: d11e758f6e38be57abf51647188d560fe335c6ce
   changedFilesBasis: branch-diff
   pullRequest: "#43"
+  subjectHeadCommit: 573796f072b40fce28017ac75d972b912c04941b
+  diffHash: sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
   headCommit: 573796f072b40fce28017ac75d972b912c04941b
 changedFiles:
-  - contracts/registry.yaml
   - contracts/spec-changes/SCP-SCWBS-041.yaml
   - contracts/tasks/SCWBS-041.yaml
   - docs/implementation-gaps.md
diff --git a/contracts/evidence/SCWBS-042.yaml b/contracts/evidence/SCWBS-042.yaml
index 6758f1c..ac22a69 100644
--- a/contracts/evidence/SCWBS-042.yaml
+++ b/contracts/evidence/SCWBS-042.yaml
@@ -2,15 +2,29 @@ id: EVD-SCWBS-042
 type: evidence
 taskId: SCWBS-042
 commit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+subjectHeadCommit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+diffHash: sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+    treeHash: 07842f8a2c500518e9544bb48ec89a863642603b
+    diffHash: sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-042.patch
+    manifestHash: sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
 git:
   branch: codex/scwbs-042-ai-next-task-evidence-filter
   base: origin/main
   baseCommit: 4270260bbb696d71bb7aec27639fbaff4eff76ad
   changedFilesBasis: branch-diff
   pullRequest: "#44"
+  subjectHeadCommit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+  diffHash: sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
   headCommit: b832efb9d0c29bb3db38729d472a296c3b8bb528
 changedFiles:
-  - contracts/registry.yaml
   - contracts/tasks/SCWBS-042.yaml
   - src/commands/ai-queue.ts
   - tests/scwbs.test.ts
diff --git a/contracts/evidence/SCWBS-043.yaml b/contracts/evidence/SCWBS-043.yaml
index 6066c89..74d1db2 100644
--- a/contracts/evidence/SCWBS-043.yaml
+++ b/contracts/evidence/SCWBS-043.yaml
@@ -2,15 +2,29 @@ id: EVD-SCWBS-043
 type: evidence
 taskId: SCWBS-043
 commit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+subjectHeadCommit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+diffHash: sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+    treeHash: 3f1cfaf303292a4c34bc8f9a02c9b16a96dc9b53
+    diffHash: sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-043.patch
+    manifestHash: sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
 git:
   branch: codex/scwbs-043-review-queue-shared-node-completion
   base: origin/main
   baseCommit: 8d15b78ef9c6d13f4b6306783c0afbc94e486333
   changedFilesBasis: branch-diff
   pullRequest: "#45"
+  subjectHeadCommit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+  diffHash: sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
   headCommit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
 changedFiles:
-  - contracts/registry.yaml
   - contracts/tasks/SCWBS-043.yaml
   - src/commands/review-queue.ts
   - tests/scwbs.test.ts
diff --git a/contracts/evidence/SCWBS-044.yaml b/contracts/evidence/SCWBS-044.yaml
index 1a23eef..dd5e612 100644
--- a/contracts/evidence/SCWBS-044.yaml
+++ b/contracts/evidence/SCWBS-044.yaml
@@ -2,15 +2,29 @@ id: EVD-SCWBS-044
 type: evidence
 taskId: SCWBS-044
 commit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+subjectHeadCommit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+diffHash: sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+    treeHash: 9f5c360ab098638dafba1a14ad8857726ed5dcae
+    diffHash: sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-044.patch
+    manifestHash: sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
 git:
   branch: codex/scwbs-044-review-health-blocker-label
   base: origin/main
   baseCommit: f5eff9a1d1c1cfa7991d3e55f22b38bde7f5d9da
   changedFilesBasis: branch-diff
   pullRequest: "#46"
+  subjectHeadCommit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+  diffHash: sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
   headCommit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
 changedFiles:
-  - contracts/registry.yaml
   - contracts/tasks/SCWBS-044.yaml
   - src/commands/review-queue.ts
   - tests/scwbs.test.ts
diff --git a/contracts/evidence/SCWBS-045.yaml b/contracts/evidence/SCWBS-045.yaml
index 0c150e4..8207b7a 100644
--- a/contracts/evidence/SCWBS-045.yaml
+++ b/contracts/evidence/SCWBS-045.yaml
@@ -2,15 +2,29 @@ id: EVD-SCWBS-045
 type: evidence
 taskId: SCWBS-045
 commit: d541daa873978f88e8d693f43e64f41a535f4c38
+subjectHeadCommit: d541daa873978f88e8d693f43e64f41a535f4c38
+diffHash: sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: d541daa873978f88e8d693f43e64f41a535f4c38
+    treeHash: ed4729cde8367c1b6ed460ad12daf9f70084d6f4
+    diffHash: sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-045.patch
+    manifestHash: sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
 git:
   branch: codex/scwbs-045-next-respects-review-blockers
   base: origin/main
   baseCommit: 3b51f1aab0edc401a68bba0f3809082622d055b0
   changedFilesBasis: branch-diff
   pullRequest: "#47"
+  subjectHeadCommit: d541daa873978f88e8d693f43e64f41a535f4c38
+  diffHash: sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
   headCommit: d541daa873978f88e8d693f43e64f41a535f4c38
 changedFiles:
-  - contracts/registry.yaml
   - contracts/tasks/SCWBS-045.yaml
   - src/commands/next.ts
   - tests/scwbs.test.ts
diff --git a/contracts/evidence/SCWBS-046.yaml b/contracts/evidence/SCWBS-046.yaml
index f2741ff..7e62a98 100644
--- a/contracts/evidence/SCWBS-046.yaml
+++ b/contracts/evidence/SCWBS-046.yaml
@@ -2,12 +2,27 @@ id: EVD-SCWBS-046
 type: evidence
 taskId: SCWBS-046
 commit: aad4134dd7d49c55cc86aface971884cc443c7cb
+subjectHeadCommit: aad4134dd7d49c55cc86aface971884cc443c7cb
+diffHash: sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: aad4134dd7d49c55cc86aface971884cc443c7cb
+    treeHash: fdba416c7ff99d57aec56fa43c13bf858b3abb2d
+    diffHash: sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-046.patch
+    manifestHash: sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
 git:
   branch: codex/scwbs-046-node-level-completion-task
   base: origin/main
   baseCommit: 80630d2b484a93b2c7871cbd071a7bf26ff0a052
   changedFilesBasis: branch-diff
   pullRequest: "#48"
+  subjectHeadCommit: aad4134dd7d49c55cc86aface971884cc443c7cb
+  diffHash: sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
   headCommit: aad4134dd7d49c55cc86aface971884cc443c7cb
 changedFiles:
   - contracts/tasks/SCWBS-046.yaml
diff --git a/contracts/evidence/SCWBS-047.yaml b/contracts/evidence/SCWBS-047.yaml
index f9c5f8f..45f43bd 100644
--- a/contracts/evidence/SCWBS-047.yaml
+++ b/contracts/evidence/SCWBS-047.yaml
@@ -2,12 +2,27 @@ id: EVD-SCWBS-047
 type: evidence
 taskId: SCWBS-047
 commit: 557390043386bbcaa92e99495b7ab3337827d6bf
+subjectHeadCommit: 557390043386bbcaa92e99495b7ab3337827d6bf
+diffHash: sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: 557390043386bbcaa92e99495b7ab3337827d6bf
+    treeHash: eb7d2c01eba36c90982ac8026730de7d7e31b9ad
+    diffHash: sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-047.patch
+    manifestHash: sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
 git:
   branch: codex/scwbs-047-node-completion-contract-metadata
   base: origin/main
   baseCommit: a77043c5fa5e6eb5522f2db792f9d65477d4cb21
   changedFilesBasis: branch-diff
   pullRequest: "#49"
+  subjectHeadCommit: 557390043386bbcaa92e99495b7ab3337827d6bf
+  diffHash: sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
   headCommit: 557390043386bbcaa92e99495b7ab3337827d6bf
 changedFiles:
   - contracts/tasks/SCWBS-046.yaml
diff --git a/contracts/evidence/SCWBS-048.yaml b/contracts/evidence/SCWBS-048.yaml
index 0451095..59aa43e 100644
--- a/contracts/evidence/SCWBS-048.yaml
+++ b/contracts/evidence/SCWBS-048.yaml
@@ -2,12 +2,27 @@ id: EVD-SCWBS-048
 type: evidence
 taskId: SCWBS-048
 commit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
+subjectHeadCommit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
+diffHash: sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
+provenance:
+  schemaVersion: 1.0.0
+  subject:
+    commit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
+    treeHash: 897aabb751977bfb402a92fb54fc1bac3388b484
+    diffHash: sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
+    canonicalization: git-diff-binary-v1
+  retention:
+    mode: patch-artifact
+    locator: repo:contracts/evidence-payloads/SCWBS-048.patch
+    manifestHash: sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
 git:
   branch: codex/scwbs-048-review-metadata-backfill
   base: origin/main
   baseCommit: 0d3a2f50f17cd219b068080eb30c9d7ecf888b6b
   changedFilesBasis: branch-diff
   pullRequest: "#50"
+  subjectHeadCommit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
+  diffHash: sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
   headCommit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
 changedFiles:
   - contracts/evidence/SCWBS-041.yaml
@@ -16,15 +31,12 @@ changedFiles:
   - contracts/evidence/SCWBS-044.yaml
   - contracts/evidence/SCWBS-045.yaml
   - contracts/evidence/SCWBS-046.yaml
-  - contracts/evidence/SCWBS-048.yaml
-  - contracts/registry.yaml
   - contracts/reviews/SCWBS-041.yaml
   - contracts/reviews/SCWBS-042.yaml
   - contracts/reviews/SCWBS-043.yaml
   - contracts/reviews/SCWBS-044.yaml
   - contracts/reviews/SCWBS-045.yaml
   - contracts/reviews/SCWBS-046.yaml
-  - contracts/reviews/SCWBS-048.yaml
   - contracts/tasks/SCWBS-048.yaml
 checks:
   - name: test
diff --git a/contracts/reviews/SCWBS-041.yaml b/contracts/reviews/SCWBS-041.yaml
index 74e5fd1..6be514a 100644
--- a/contracts/reviews/SCWBS-041.yaml
+++ b/contracts/reviews/SCWBS-041.yaml
@@ -1,9 +1,26 @@
 id: RVW-SCWBS-041
 type: review
 taskId: SCWBS-041
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: 573796f072b40fce28017ac75d972b912c04941b
+diffHash: sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8
 pullRequest: "#43"
 groundTruth:
   - contracts/tasks/SCWBS-041.yaml
   - contracts/evidence/SCWBS-041.yaml
+requestedReviewers:
+  - role: methodology-owner
+    user: unassigned
+    reason: SC-WBS contracts or methodology documents changed
+  - role: code-owner
+    user: unassigned
+    reason: source or test files changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:05:03.910Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #43'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/reviews/SCWBS-042.yaml b/contracts/reviews/SCWBS-042.yaml
index 83f4c65..b5ce4dd 100644
--- a/contracts/reviews/SCWBS-042.yaml
+++ b/contracts/reviews/SCWBS-042.yaml
@@ -1,9 +1,26 @@
 id: RVW-SCWBS-042
 type: review
 taskId: SCWBS-042
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: b832efb9d0c29bb3db38729d472a296c3b8bb528
+diffHash: sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645
 pullRequest: "#44"
 groundTruth:
   - contracts/tasks/SCWBS-042.yaml
   - contracts/evidence/SCWBS-042.yaml
+requestedReviewers:
+  - role: methodology-owner
+    user: unassigned
+    reason: SC-WBS contracts or methodology documents changed
+  - role: code-owner
+    user: unassigned
+    reason: source or test files changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:05:17.521Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #45'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/reviews/SCWBS-043.yaml b/contracts/reviews/SCWBS-043.yaml
index a6f2f6b..bed375c 100644
--- a/contracts/reviews/SCWBS-043.yaml
+++ b/contracts/reviews/SCWBS-043.yaml
@@ -1,9 +1,26 @@
 id: RVW-SCWBS-043
 type: review
 taskId: SCWBS-043
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0
+diffHash: sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63
 pullRequest: "#45"
 groundTruth:
   - contracts/tasks/SCWBS-043.yaml
   - contracts/evidence/SCWBS-043.yaml
+requestedReviewers:
+  - role: methodology-owner
+    user: unassigned
+    reason: SC-WBS contracts or methodology documents changed
+  - role: code-owner
+    user: unassigned
+    reason: source or test files changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:07:07.195Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #45'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/reviews/SCWBS-044.yaml b/contracts/reviews/SCWBS-044.yaml
index 6c44110..7df3548 100644
--- a/contracts/reviews/SCWBS-044.yaml
+++ b/contracts/reviews/SCWBS-044.yaml
@@ -1,9 +1,26 @@
 id: RVW-SCWBS-044
 type: review
 taskId: SCWBS-044
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: 33f7f918b0a1ef648ce917bde7111a27781ad1ff
+diffHash: sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21
 pullRequest: "#46"
 groundTruth:
   - contracts/tasks/SCWBS-044.yaml
   - contracts/evidence/SCWBS-044.yaml
+requestedReviewers:
+  - role: methodology-owner
+    user: unassigned
+    reason: SC-WBS contracts or methodology documents changed
+  - role: code-owner
+    user: unassigned
+    reason: source or test files changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:05:27.621Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #46'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/reviews/SCWBS-045.yaml b/contracts/reviews/SCWBS-045.yaml
index 86f6222..6b81bce 100644
--- a/contracts/reviews/SCWBS-045.yaml
+++ b/contracts/reviews/SCWBS-045.yaml
@@ -1,9 +1,26 @@
 id: RVW-SCWBS-045
 type: review
 taskId: SCWBS-045
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: d541daa873978f88e8d693f43e64f41a535f4c38
+diffHash: sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a
 pullRequest: "#47"
 groundTruth:
   - contracts/tasks/SCWBS-045.yaml
   - contracts/evidence/SCWBS-045.yaml
+requestedReviewers:
+  - role: methodology-owner
+    user: unassigned
+    reason: SC-WBS contracts or methodology documents changed
+  - role: code-owner
+    user: unassigned
+    reason: source or test files changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:05:42.303Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #47'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/reviews/SCWBS-046.yaml b/contracts/reviews/SCWBS-046.yaml
index b6d4f62..8e2bc25 100644
--- a/contracts/reviews/SCWBS-046.yaml
+++ b/contracts/reviews/SCWBS-046.yaml
@@ -1,9 +1,26 @@
 id: RVW-SCWBS-046
 type: review
 taskId: SCWBS-046
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: aad4134dd7d49c55cc86aface971884cc443c7cb
+diffHash: sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b
 pullRequest: "#48"
 groundTruth:
   - contracts/tasks/SCWBS-046.yaml
   - contracts/evidence/SCWBS-046.yaml
+requestedReviewers:
+  - role: methodology-owner
+    user: unassigned
+    reason: SC-WBS contracts or methodology documents changed
+  - role: code-owner
+    user: unassigned
+    reason: source or test files changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:05:51.875Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #48'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/reviews/SCWBS-047.yaml b/contracts/reviews/SCWBS-047.yaml
index 2788364..593e5fe 100644
--- a/contracts/reviews/SCWBS-047.yaml
+++ b/contracts/reviews/SCWBS-047.yaml
@@ -1,8 +1,10 @@
 id: RVW-SCWBS-047
 type: review
 taskId: SCWBS-047
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: 557390043386bbcaa92e99495b7ab3337827d6bf
+diffHash: sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e
 pullRequest: "#49"
 groundTruth:
   - contracts/tasks/SCWBS-047.yaml
@@ -11,3 +13,11 @@ requestedReviewers:
   - role: methodology-owner
     user: unassigned
     reason: SC-WBS contracts or methodology documents changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:06:02.710Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #49'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/reviews/SCWBS-048.yaml b/contracts/reviews/SCWBS-048.yaml
index 6a12afa..5a97b04 100644
--- a/contracts/reviews/SCWBS-048.yaml
+++ b/contracts/reviews/SCWBS-048.yaml
@@ -1,8 +1,10 @@
 id: RVW-SCWBS-048
 type: review
 taskId: SCWBS-048
-status: requested
+status: approved
 reviewProfile: independent-ai-review
+headCommit: ee504c805f41276bb1f2981d43b4a39db4a0f4c9
+diffHash: sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa
 pullRequest: "#50"
 groundTruth:
   - contracts/tasks/SCWBS-048.yaml
@@ -11,3 +13,11 @@ requestedReviewers:
   - role: methodology-owner
     user: unassigned
     reason: SC-WBS contracts or methodology documents changed
+reviewedBy: human
+reviewedAt: "2026-08-24T19:06:18.970Z"
+findings:
+  - Verified migrated Evidence identity
+  - retained payload
+  - 'PR #50'
+  - subjectHeadCommit
+  - and diffHash
diff --git a/contracts/specs/SPEC-LITE-MT7ICSMN.yaml b/contracts/specs/SPEC-LITE-MT7ICSMN.yaml
new file mode 100644
index 0000000..fd36044
--- /dev/null
+++ b/contracts/specs/SPEC-LITE-MT7ICSMN.yaml
@@ -0,0 +1,28 @@
+id: SPEC-LITE-MT7ICSMN
+type: spec-contract
+featureId: F-MT7ICSMN
+title: Atomic SCWBS-041 through SCWBS-048 lifecycle subject migration
+status: approved
+version: 0.1.0
+approvedBy: user-provided-remediation-objective
+approvedAt: "2026-08-25T02:26:40+09:00"
+summary: Atomically migrate the eight legacy node-completion cohort subjects to canonical retained Evidence, then obtain independent human Review and scoped post-finish v2 Approval without changing source, WBS, or Task authority.
+acceptanceCriteria:
+  - "SCWBS-041: PR 43, subject 573796f072b40fce28017ac75d972b912c04941b, diffHash sha256:519b67f535c53b5af33abc547f6a49c2362b9a4eaea8736298dba0ff546475b8."
+  - "SCWBS-042: PR 44, subject b832efb9d0c29bb3db38729d472a296c3b8bb528, diffHash sha256:cdff4722d4301f395c101f5efcdb9034e8649d727152968bcc3646d104aab645."
+  - "SCWBS-043: PR 45, subject 9e06e3dcdff2270cb43a976e10e3fb45ed83b9b0, diffHash sha256:64fca6d7020f9107c73f16ad2c24d5aefe3d2c39d33801d34fb0dfa7eb0c0e63."
+  - "SCWBS-044: PR 46, subject 33f7f918b0a1ef648ce917bde7111a27781ad1ff, diffHash sha256:657f8d1ec21acadc9fbdccca858ad4b85f6eedd7a6d63f6b49ad56147edade21."
+  - "SCWBS-045: PR 47, subject d541daa873978f88e8d693f43e64f41a535f4c38, diffHash sha256:583c77b2c50a69a5b0f54d2b36c1c355422cfc6c5dd82385421a5f382fe0580a."
+  - "SCWBS-046: PR 48, subject aad4134dd7d49c55cc86aface971884cc443c7cb, diffHash sha256:5eb49b6afa32f6e074d3b36ed6e819e835d82d9fb949540a37853942762fad3b."
+  - "SCWBS-047: PR 49, subject 557390043386bbcaa92e99495b7ab3337827d6bf, diffHash sha256:77c1ddc36c117b88dd93034caa0b7a4246f6a9ce0ab41ac891da1ee0ec22c21e."
+  - "SCWBS-048: PR 50, subject ee504c805f41276bb1f2981d43b4a39db4a0f4c9, diffHash sha256:10f4801ca9ad6f4302c6cb3a6d2f09708a7fd5692aebbcfbb95f5fc4ab62a3fa."
+  - Every authoritative dry-run verifies historical base and subject objects, origin PR ancestry, canonical changedFiles, git-diff-binary-v1 diffHash, and untruncated output before any tracked mutation.
+  - Evidence retention preserves historical checks and testQuality facts, creates reproducible patch payload provenance, and changes no source, test, documentation, WBS, changeset, or existing Task Contract.
+  - Review requests are replaced only after all eight Evidence subjects are stable; AI never approves, rejects, closes, or marks changes requested.
+  - Post-finish v2 Approval requests are created for the historical PR of each subject, and only human TTY approvals with exact current Evidence identity satisfy readiness.
+  - "The cohort is all-or-nothing: any mismatch or human rejection blocks the migration Task and no partial migration is merged."
+nonGoals:
+  - Changing implementation source, tests, documentation, schemas, package configuration, CI, authentication, WBS, changesets, or Task authority.
+  - Reusing SCWBS-053 or an existing node-completion changeset.
+  - Performing Review or Approval decisions on behalf of a human.
+  - Completing the WBS node or refreshing the 227 Task contract locks affected by a future node status transition.
diff --git a/contracts/tasks/SCWBS-DRAFT-MT7ICSMN.yaml b/contracts/tasks/SCWBS-DRAFT-MT7ICSMN.yaml
new file mode 100644
index 0000000..ee12296
--- /dev/null
+++ b/contracts/tasks/SCWBS-DRAFT-MT7ICSMN.yaml
@@ -0,0 +1,145 @@
+id: SCWBS-DRAFT-MT7ICSMN
+type: task-contract
+wbsNodeId: node-governance-maintenance
+featureId: F-MT7ICSMN
+branchName: task/SCWBS-DRAFT-MT7ICSMN-migrate-scwbs-041-through-scwbs-048-lifecycle-su
+allowedPaths:
+  - contracts/evidence/SCWBS-041.yaml
+  - contracts/evidence-payloads/SCWBS-041.patch
+  - contracts/reviews/SCWBS-041.yaml
+  - contracts/approvals/SCWBS-041.yaml
+  - contracts/evidence/SCWBS-042.yaml
+  - contracts/evidence-payloads/SCWBS-042.patch
+  - contracts/reviews/SCWBS-042.yaml
+  - contracts/approvals/SCWBS-042.yaml
+  - contracts/evidence/SCWBS-043.yaml
+  - contracts/evidence-payloads/SCWBS-043.patch
+  - contracts/reviews/SCWBS-043.yaml
+  - contracts/approvals/SCWBS-043.yaml
+  - contracts/evidence/SCWBS-044.yaml
+  - contracts/evidence-payloads/SCWBS-044.patch
+  - contracts/reviews/SCWBS-044.yaml
+  - contracts/approvals/SCWBS-044.yaml
+  - contracts/evidence/SCWBS-045.yaml
+  - contracts/evidence-payloads/SCWBS-045.patch
+  - contracts/reviews/SCWBS-045.yaml
+  - contracts/approvals/SCWBS-045.yaml
+  - contracts/evidence/SCWBS-046.yaml
+  - contracts/evidence-payloads/SCWBS-046.patch
+  - contracts/reviews/SCWBS-046.yaml
+  - contracts/approvals/SCWBS-046.yaml
+  - contracts/evidence/SCWBS-047.yaml
+  - contracts/evidence-payloads/SCWBS-047.patch
+  - contracts/reviews/SCWBS-047.yaml
+  - contracts/approvals/SCWBS-047.yaml
+  - contracts/evidence/SCWBS-048.yaml
+  - contracts/evidence-payloads/SCWBS-048.patch
+  - contracts/reviews/SCWBS-048.yaml
+  - contracts/approvals/SCWBS-048.yaml
+forbiddenPaths:
+  - wjs/**
+  - contracts/wbs/**
+  - contracts/changesets/**
+  - contracts/discovery/**
+  - contracts/tasks/SCWBS-0**
+  - contracts/spec-changes/**
+  - src/**
+  - tests/**
+  - docs/**
+  - scripts/**
+  - .github/**
+  - package.json
+  - package-lock.json
+  - tsconfig.json
+  - vitest.config.ts
+humanGateRequiredPaths:
+  - package.json
+  - package-lock.json
+  - tsconfig.json
+  - vitest.config.ts
+  - .github/**
+  - contracts/evidence/SCWBS-041.yaml
+  - contracts/evidence-payloads/SCWBS-041.patch
+  - contracts/reviews/SCWBS-041.yaml
+  - contracts/approvals/SCWBS-041.yaml
+  - contracts/evidence/SCWBS-042.yaml
+  - contracts/evidence-payloads/SCWBS-042.patch
+  - contracts/reviews/SCWBS-042.yaml
+  - contracts/approvals/SCWBS-042.yaml
+  - contracts/evidence/SCWBS-043.yaml
+  - contracts/evidence-payloads/SCWBS-043.patch
+  - contracts/reviews/SCWBS-043.yaml
+  - contracts/approvals/SCWBS-043.yaml
+  - contracts/evidence/SCWBS-044.yaml
+  - contracts/evidence-payloads/SCWBS-044.patch
+  - contracts/reviews/SCWBS-044.yaml
+  - contracts/approvals/SCWBS-044.yaml
+  - contracts/evidence/SCWBS-045.yaml
+  - contracts/evidence-payloads/SCWBS-045.patch
+  - contracts/reviews/SCWBS-045.yaml
+  - contracts/approvals/SCWBS-045.yaml
+  - contracts/evidence/SCWBS-046.yaml
+  - contracts/evidence-payloads/SCWBS-046.patch
+  - contracts/reviews/SCWBS-046.yaml
+  - contracts/approvals/SCWBS-046.yaml
+  - contracts/evidence/SCWBS-047.yaml
+  - contracts/evidence-payloads/SCWBS-047.patch
+  - contracts/reviews/SCWBS-047.yaml
+  - contracts/approvals/SCWBS-047.yaml
+  - contracts/evidence/SCWBS-048.yaml
+  - contracts/evidence-payloads/SCWBS-048.patch
+  - contracts/reviews/SCWBS-048.yaml
+  - contracts/approvals/SCWBS-048.yaml
+stopIf:
+  - any migration dry-run is blocked or truncated
+  - PR base or subject ancestry cannot be authoritatively verified
+  - non-lifecycle changedFiles drift appears
+  - evidence retain produces a mismatch or writes outside exact scope
+  - historical fields beyond declared subject changedFiles and provenance would change
+  - any Review or Approval transition would be performed by AI
+  - any Review or Approval is rejected changes-requested non-human or subject-mismatched
+  - source WBS changeset Task authority or schema change is required
+  - authority expansion is required
+  - partial cohort migration would be merged
+requiredChecks:
+  - test
+  - typecheck
+  - build
+  - test:integration
+doneCriteria:
+  - Immediately before mutation, authoritative migration dry-runs for SCWBS-041 through SCWBS-048 are ready with no blockers or truncation and exactly match the approved subject, pull request, changedFiles, and diffHash matrix in the Spec.
+  - Each target Evidence records canonical subjectHeadCommit and diffHash at the top level and in git metadata, canonical changedFiles, and reproducible patch-artifact provenance while preserving historical branch, base, pull request, checks, and test-quality facts.
+  - Each retained payload reproduces the recorded implementation subject from its historical base in a fresh clone and passes existing Evidence verification.
+  - Each target Review is independently requested and then human-approved with taskId, pull request, headCommit, and diffHash exactly matching current Evidence.
+  - Each target Approval is a scoped v2 post-finish human Approval with lean TTY provenance and exact Evidence pull request, headCommit, and diffHash; legacy unscoped fallback is not accepted.
+  - Review and Approval approval operations remain human-only, and AI performs request operations only after the migrated Evidence subjects are stable.
+  - Shared completion readiness reports no Evidence, Review, pull request, subject, or post-finish Approval blocker for SCWBS-041 through SCWBS-048.
+  - Only the 32 exact cohort artifact paths and this Task's managed contract paths change; no partial cohort migration is merged.
+  - Unit, typecheck, build, full integration, diff guard, registry, Human Gate, post-finish Approval, CI, and merge checks pass.
+evidenceRequired:
+  - test-result
+  - typecheck-result
+  - build-result
+managedContractPaths:
+  - contracts/tasks/SCWBS-DRAFT-MT7ICSMN.yaml
+  - contracts/tasks/index.yaml
+  - contracts/specs/SPEC-LITE-MT7ICSMN.yaml
+  - contracts/blocks/SCWBS-DRAFT-MT7ICSMN.yaml
+  - contracts/evidence/SCWBS-DRAFT-MT7ICSMN.yaml
+  - contracts/evidence-payloads/SCWBS-DRAFT-MT7ICSMN.patch
+  - contracts/approvals/SCWBS-DRAFT-MT7ICSMN.yaml
+  - contracts/reviews/SCWBS-DRAFT-MT7ICSMN.yaml
+  - contracts/registry.yaml
+specPath: contracts/specs/SPEC-LITE-MT7ICSMN.yaml
+specVersion: 0.1.0
+status: approved
+approvedBy: user-provided-remediation-objective
+approvedAt: "2026-08-25T02:26:40+09:00"
+contractLock:
+  lockVersion: "2"
+  wbsScopeRevision: sha256:1a15147d66e7b7e66dadc0e3a3c333baf850c33d938a72bae0802619d3cb89fa
+  wbsGlobalRevision: sha256:1ec3e452163365de9e473830ef3c13ac021f4ac1df8050c9c5ccc58e78d54612
+  wbsNodeId: node-governance-maintenance
+  specVersion: 0.1.0
+  specRevision: sha256:4aad6ea73543309a2af7e55721e19171f22ef383659b651a21ace9ea6072d7ba
+  createdAt: "2026-08-24T17:42:15.316Z"
diff --git a/contracts/tasks/index.yaml b/contracts/tasks/index.yaml
index dc257bb..8791920 100644
--- a/contracts/tasks/index.yaml
+++ b/contracts/tasks/index.yaml
@@ -1817,3 +1817,9 @@ tasks:
     wbsNodeId: node-governance-maintenance
     status: planned
     dependsOn: []
+  - id: SCWBS-DRAFT-MT7ICSMN
+    path: contracts/tasks/SCWBS-DRAFT-MT7ICSMN.yaml
+    branchName: task/SCWBS-DRAFT-MT7ICSMN-migrate-scwbs-041-through-scwbs-048-lifecycle-su
+    wbsNodeId: node-governance-maintenance
+    status: planned
+    dependsOn: []
