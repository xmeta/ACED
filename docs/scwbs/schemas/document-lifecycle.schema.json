{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/xmeta/ACED/blob/main/docs/scwbs/schemas/document-lifecycle.schema.json",
  "title": "SC-WBS document lifecycle manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "standardEntrypoints", "documents", "maxLines", "qualityExceptions"],
  "properties": {
    "schemaVersion": { "const": "1.1.0" },
    "maxLines": { "type": "integer", "minimum": 1 },
    "qualityExceptions": {
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/qualityException" }
    },
    "standardEntrypoints": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/repositoryPath" }
    },
    "ignoredPaths": {
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/repositoryPath" }
    },
    "documents": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/documentSet" }
    }
  },
  "$defs": {
    "repositoryPath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?![A-Za-z]:)(?!.*(?:^|/)\\.\\.?(?:/|$)).+$"
    },
    "documentSet": {
      "type": "object",
      "additionalProperties": false,
      "required": ["documentId", "status", "version", "appliesToCli", "entrypoint", "paths", "supersedes", "language"],
      "properties": {
        "documentId": { "type": "string", "minLength": 1 },
        "status": { "enum": ["normative", "informative", "proposal", "deprecated", "superseded"] },
        "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
        "appliesToCli": {
          "type": "string",
          "pattern": "^(?:(?:>=|<=|>|<|=)?\\d+\\.\\d+\\.\\d+)(?:\\s+(?:>=|<=|>|<|=)?\\d+\\.\\d+\\.\\d+)*$"
        },
        "entrypoint": { "$ref": "#/$defs/repositoryPath" },
        "paths": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/repositoryPath" }
        },
        "supersedes": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "language": { "enum": ["ja", "en"] }
      }
    },
    "qualityException": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "reason", "owner", "expiresAt"],
      "properties": {
        "path": { "$ref": "#/$defs/repositoryPath" },
        "reason": { "type": "string", "minLength": 1 },
        "owner": { "type": "string", "minLength": 1 },
        "expiresAt": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" }
      }
    }
  }
}
